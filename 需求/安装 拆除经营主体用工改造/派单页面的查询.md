/list-package-by-operator-manager
主要关注的
	入参为「施工包状态」
	出参为「任务列表」

首先查询这个角色下所属的作业类型列表，并返回销售工人的ID列表。提取工人列表，并识别传入的状态列表是否为空，若为空就按默认的状态来给。- 默认查询活跃状态的包（用户未指定时，查询处理中、待上门、已完成的包）。
并根据工人列表和状态列表，以及关键字和城市code查询。
之后进行数据转换:
```markdown
该方法遍历 `extraFieldTypeInfoList`，根据 `type` 字段将 `valueList` 分配到对应的字段：

- **type 1** → `packageProblemFormList`（问题单）
    
- **type 2** → `reappointProblemFormList`（在审核约工驳回申请单）
    
- **type 3** → `packageSecondStatusList`（施工包二级状态）
    
- **type 4** → `extraQuotaAuditingFlag`（额外配额审核标志）
```

