# 📚 向编程助手提需求的「首次提问信息清单」

> **排序原则：越靠前 = 不填越容易导致返工或人工大改。**
> 每条标注 **防 D#**，对应《📚 纯零售接入排程改造复盘（方案vs实现差异）》中的真实差异项——每条都不是理论，是实际返工过的。
> **实测基准**：该需求返工 6+ 轮、编译失败 2 次、"改了又删"约 44 行、静默失效风险 3 项；按本清单给全信息后预期 **1–2 轮**。

---

## A. 必填：不填 = 必然返工（防 D1–D8）

1. **配置与接口的精确契约** — 写死字符串，不写概念：Apollo key 全名、字段名与语义、接口入参/返回结构。〔防 D1、D3〕
   > **不填会怎样**：D1 三套 key 并存（`switch` / `switch.companyList`），配错即**静默失效**不报错；D3 接口入参 Param 对象 vs `List<String>` 二选一错，调用方全部要跟着改。
   > 填：`Apollo key=__；字段：__=__，__=__；接口 __ 入参=__，返回=__`

2. **开关/灰度的准确语义** — 每个字段代表什么、多值如何判定（AND/OR）、未配置时默认放行还是不放行、怎么回滚。〔防 D2、D4〕
   > **不填会怎样**：D2 我做成单值 `contains`，实现做成 `containsAll`(AND)，多分公司场景**全不放行**；D4 没定义"未命中是否回退"，导致上线即降级、无法回滚。
   > 填：`"灰度"定义=__（逐步开分公司 / 流量百分比）；多值判定=AND/OR；未配置默认=放行/不放行；回滚方式=__`

3. **改造前逐场景实际行为 + 幂等范围** — 用「场景 → 当前路径」写现状；明确哪些场景必须改动前后完全一致（入参/调用次数/返回/日志）。〔防 D5、D9〕
   > **不填会怎样**：D5 批量接口被改成"12 项先返回、其余后追加"，**破坏了原本与入参一一对应的顺序**；D9 判定"天然兼容"却无验收标准，实际从未验证。
   > 填：`| 场景 | 当前路径 |　例：configFlag=true→__；false→__；必须幂等：__`

4. **全部改动点清单（含同源方法）** — 不只点主入口，要列出所有同名/同源调用点与分支场景（有/无订单、单值/多值）。〔防 D8〕
   > **不填会怎样**：D8 只点名 `createByDelivery`，`repairCreateByDelivery` **漏改**，补建任务路径完全没覆盖，功能缺一大块却没人发现。
   > 填：`改动点：__:__#__；同源方法：__；分支场景：有/无__、单值/多值__`

5. **分支与基线** — 在哪个分支改、基线是什么、是否已有 commit、哪些历史实现禁止参考。〔防返工轮次 1〕
   > **不填会怎样**：我去读旧分支既有实现被叫停，整轮探查作废；用 `git show HEAD:` 当原版，结果 HEAD 已含本次改动，走查结论全错。
   > 填：`分支=__（相对 master 已有 __ 个 commit，基线用 master 不用 HEAD）；禁止复用=__`

## B. 强建议：不填 = 实现方式易走样（防 D7、D10）

6. **跨模块/外部依赖与验证边界** — 涉及枚举推导、外部 jar 的，写明依赖链路并标注"源码不可见需实测"。〔防 D7〕
   > **不填会怎样**：D7 未追到 `calculateDocumentType→外部枚举` 链路，漏改 `DocumentTypeEnum`，纯零售 documentType **推不出 12 → 功能整体失效**；连带改 base 还要发 SNAPSHOT，方案完全没提。
   > 填：`依赖链路：__→__；外部不可见需实测：__；改公共模块的版本/发布影响：__`

7. **改动边界** — 只允许改哪些文件、是否禁止顺带优化、能否改公共模块。〔防 D10〕
   > **不填会怎样**：D10 混入排期查询缓存优化（+39 行）、pom 版本改 SNAPSHOT，diff 变大、评审与发布风险上升，需求边界模糊。
   > 填：`只允许改：__；禁止顺带优化=是/否；允许改公共模块=是/否`

8. **现有代码结构（文件:行号#方法 + 参照实现）** — 给确切位置与"仿照哪个既有实现"，比文字描述准。
   > 填：`改动点：__:__#__；参考实现：仿 __#__`

9. **技术栈与构建命令** — 语言/框架/版本、构建运行命令、模块依赖方向与 DTO 放置约定。
   > **不填会怎样**：对外 DTO 放错包直接编译失败（本次 2 次，各占 1 轮）。
   > 填：`__+__；构建：__；依赖方向：__；对外 DTO 放 __，内部 BO 放 __`

## C. 建议：影响交付效率

10. **验收标准** — 可判定的完成条件（命令、接口返回、对比结果）。〔防 D9〕
    > 填：`完成条件：__ 编译通过；__ 接口返回 __；与改造前对比：__`
11. **输出形式与已知坑** — diff/完整文件；先给方案还是直接改；是否 git add（默认不 commit）；修 bug 附报错与复现。
    > 填：`输出=__；先方案后改=__；git add=是（不 commit）；报错：__`

---

## 附：空白模板（复制即用）

```markdown
## 1 配置与接口契约（防D1/D3）
Apollo key=__；字段：__=__，__=__；接口 __ 入参=__，返回=__

## 2 开关/灰度语义（防D2/D4）
"灰度"定义=__；多值判定=AND/OR；未配置默认=放行/不放行；回滚方式=__

## 3 改造前行为与幂等（防D5/D9）
| 场景 | 当前路径 |
|---|---|
| __ | __ |
必须幂等（入参/调用/返回/日志一致）：__

## 4 全部改动点（防D8）
改动点：__:__#__；同源方法：__；分支场景：有/无__、单值/多值__

## 5 分支与基线
分支=__（相对 master 已有 __ 个 commit，基线用 master 不用 HEAD）；禁止复用=__

## 6 外部依赖与验证边界（防D7）
依赖链路：__→__；外部不可见需实测：__；改公共模块版本/发布影响：__

## 7 改动边界（防D10）
只允许改：__；禁止顺带优化=__；允许改公共模块=__

## 8 现有代码结构
改动点：__:__#__；参考实现：仿 __#__

## 9 技术栈
__+__；构建：__；依赖方向：__；对外 DTO 放 __

## 10 验收标准
__

## 11 输出形式与已知坑
输出=__；先方案后改=__；git add=是（不commit）；报错：__
```

---

## 效果预期（基于本次实测）

| 指标 | 未用清单（实测） | 用清单后（预期） |
|---|---|---|
| 返工轮次 | 6+ | 1–2 |
| 编译失败 | 2 次 | 0 |
| "改了又删"代码量 | 约 44 行 | 约 0 |
| 静默失效风险项 | 3 项 | 0–1 项 |
| 人工补充/修改范围 | 涉及 10+ 文件、含 4 处逻辑修正 | 仅业务决策类补充 |

**一句话心法**：把你脑子里的"理所当然"写下来——key 的全名、改造前到底怎么跑的、"灰度"在本项目指什么、哪些同源方法要一起改、哪些场景绝对不能变。**写死字符串，不写概念。**

---

## ✅ 正向验证：一次通过的重构（2026-09-09 queryConfigOFCList）

**对照实验**：同一文件、同一批代码，前几轮返工 6+ 次；本次重构**一次编译通过、零返工**。差异不在代码能力，在流程：

| 本次做了什么 | 对应清单项 |
|---|---|
| 需求方指令本身就是"清单式"的：给了精确实现约束（for 放外层、内部判 configFlag、不抽方法、documentType 统一并入 list、`[1,12,16]` 行为说明）——**写死了结构，没写概念** | 清单全文 |
| AI 动手前先**复述理解 + 一次问清 9 个歧义点**（参考方法确认 / configFlag=true 是否判开关 / 顺序 / 合并去重 / 拼接方式 / 返回行数 / 取码来源 / 空值兜底 / 改动范围） | A1–A5（必填项） |
| 交付前先给**可评审的完整伪代码**，需求方批准后再落地 | C10 验收标准 |
| 语义已在前几轮摸清（documentType 单值/列表、开关语义），本次是**结构调整而非语义设计**，复杂度天然低 | B8/B9 |

**为什么这次不用返工——四条可复用经验**：

1. **返工的根源是"没说清就写"，不是代码能力**。返工轮次里没有一次是"AI 看不懂"，全是"AI 不知道你想的细节"；一次把细节问完（9 个问题）≈ 省 3–4 轮试错。
2. **需求方的指令本身就是最好的模板**：连"不抽方法""不去重""维持先新后旧"这种约束都写清——即"**写死字符串，不写概念**"。此前 `all` / `grayRatio` 返工都是因为只说"加个开关/灰度"，没说字段与语义。
3. **Ask 模式（先问后动）是优势不是限制**：它强制"先对齐 → 再方案 → 后落地"，天然踩刹车，避免"边猜边写"。流程顺序比速度重要。
4. **前几轮返工不是白费的**：documentType 单值/列表、开关语义已在前期摸透并沉淀，使本次只剩"结构调整"——语义问题清空后，后续改动复杂度天然降低。

**一句话总结**：能一次通过，是因为"该问的一次问清 + 要的约束一次给全"——把返工成本前置成了提问成本。


```
日常开发 将纯零售业务接入排程模式（整装已接入）。硬性约束（最高优先级）：仅迁移纯零售，不得修改其他业务逻辑，保持完全兼容。纯零售以 DocumentType=12 标识，计算方法在代码中自行定位。1. 新增分公司灰度开关：满足「零售&&全国&&指定分公司」进入排程。暴露接口 /api/config/pureRetailScheduleSwitch；分公司白名单用 Apollo 配置 pure.retail.schedule.switch.company.list 维护，另需全国配置。2. 排程整装页面兼容纯零售，接口：激活时间配置（delivery/flow/query/selected/activate/condition，POST）、节点角色配置（/delivery/flow/task/role/query）、最早可上门时间（/delivery/flow/task/time/condition/level1，POST）、送货流程查询（/task/process/template/list，POST）。判断各接口是否已兼容，仅对必要接口最小改动。3. 其他系统查零售配置接口改为查排程零售配置。4. 主材申请单、货查询配置：加分公司开关，业务类型=纯定软单，接口 /delivery/process/query-category-config-list、/delivery/process/query-config-list；确定订单类型判断方式。`com.ke.utopia.servicev2.deliveryflow.impl.ConfigQueryServiceImpl#queryConfigOFCList` 方法L260行以后。

背景：每个 OFC Param 有两个取值来源——Document Type（单值）和 DocumentType List（多值集合）。"纯定软店"编码为 12，命中 12 的参数走新逻辑，未命中的走原有老逻辑。

实现以下分支：
1. Document Type 存在且 DocumentType List 为空：仅依据 Document Type 判断，等于 12 走新逻辑，不等于 12 走老逻辑。
2. Document Type 不存在且 DocumentType List 非空：遍历列表，将值为 12 的项单独拆分出来走新逻辑，其余项（如 1、16）合并走老逻辑；列表不含 12 时整体走老逻辑。
3. Document Type 与 DocumentType List 同时存在、或两者都不存在的情况，不考虑

约束：
- 将 12 定义为语义化常量（如 RETAIL_REGULAR_SD_RETAIL_ORDER），避免硬编码。
- 新老逻辑的结果按 OFC Param 维度合并返回，保持原有返回结构、字段与顺序不变。
- 方法出入参签名与异常处理方式保持不变，改动范围限定在该方法内部。

5. 中控 material-task/install-role/list：在 TaskTemplateRoleServiceImpl#getTaskMode 按分公司+业务类型灰度。6. 材料配置 /material-task/template/list-all-compatibility、/material-task/template/list-all：在 TaskTemplateServiceImpl#convert2NMaterialTemplateUnitParam 确定 mode 时按分公司+业务类型灰度。7. 材料任务单任务生成 MaterialCreateV2ServiceImpl#buildCondition：支持纯零售，关注 saleType、DocumentType。8. 货单触发任务生成 DeliveryMaterialBizServiceImpl#createByDelivery：确定 mode 时按分公司+业务类型灰度；无订单视为零售，全国2.5且命中分公司走排程。不要参考之前的方案重新开发
```


开关规则，明确是任意匹配还是全部匹配
com.ke.utopia.enumeration.deliveryflow.DocumentTypeEnum#getByBusinessTypeAndSecondTypeSupportComplex类型判断未加
false的情况未处理


| 根因                 | 对应缺失项         | 题面当时怎么写的 → AI 读成了什么                                                                                               |
| ------------------ | ------------- | ----------------------------------------------------------------------------------------------------------------- |
| ① **题面语义与基准相反**    | F1 开关语义       | 「零售**&&**全国**&&**指定分公司」→ AI 实现 AND；基准是"全国开就短路放行"的 OR。**用 `&&` 表达三条件并列，无法区分逻辑与还是优先级**                              |
| ② **题面给了与基准不同的常量** | F2 Apollo key | 题面给 `...company.list` +「另需全国配置」→ AI 归纳成单 key `pure.retail.schedule.switch`；基准是 `...companyList`。**题面常量本身就不是基准常量** |
| ③ **题面是开放指令、不是规格** | F4/F5/F6      | 「**确定**订单类型判断方式」→ AI 自己定了一种（只用于分源判定）；基准改了两处既有转换                                                                   |
| ④ **题面完全没提该落点**    | F3            | 题面项 6 只点 `convert2NMaterialTemplateUnitParam`，**从未提** `buildDeliveryFlowTaskTemplateDTO`。AI 无法推断同一入口下游还要改         |
| ⑤ **题面给了落点但没给锚点**  | F7、F8         | 「在 `convert2NMaterialTemplateUnitParam` **确定 mode 时**」→ AI 就插在"确定 mode"处（方法前段），基准在方法末尾（套餐清空之后）。两种读法都合理，但行为不同      |
| ⑥ **题面缺新概念的定义**    | F4 的条数与顺序契约   | 题面从未出现"分源/两段/非 12 走旧逻辑"这些词 → AI 做成了"整参走新"                                                                         |
| ⑦ **题面范围 ≠ 基准范围**  | F9、F14        | 题面项 7 要求改 `MaterialCreateV2ServiceImpl`（基准没改）；题面没提 pom（基准改了）                                                      |

为什么这么慢？


模型：deepseek-v4-flash
核心指标：ai沟通轮数、代码返工占比

1.技术方案语言与AI理解偏差
	例如：开关为「零售&&**全国**&&指定分公司」只看这个很容易理解为有全国的开关和分公司的开关，全国的开关开启，分公司白名单就不起作用。但实测3次不同上下文中ai均理解为全国和指定分公司为AND关系即全国开启才会用分公司开关。**用 `&&` 表达三条件并列，无法区分逻辑与还是优先级**
2.指令中对某一方法修改时根据现有的单据类型判断方式进行判断，三次指令中一次未说是现有的方式，两次说了是现有的方法，但最终结果均是AI自己定了一个方式
3.对某一接口逻辑开发时指定方法和接口后AI会优先修改这个指定接口但是无法推断统一入口下游要不要修改。（或许明确入口后，在声明用逻辑来验证）
4.在零售切排程时针对不同条件走不同路径查询时，指令说明进行零售的迁移查询零售的配置改为查询排程的，未指明具体根据什么入参开来区分 AI经常修改不到位只修改一条路径或者全部修改为一条路径。尝试指令中说明其他单据类型的业务规则和逻辑保证与之前修改的相等，这种相对模糊的指令AI无法识别

第一次开发共计8轮ai开发，以及约34%的人工返工率
使用优化后的模板指令2轮ai开发以及约16%的代码
本次以纯零售迁移为例，重点是  2 3 以及 4 

```
【1 改哪里】
- 改：`类#方法`、`类#方法`   ← 一行一个，到方法粒度
- 不要动：`类`、`类`、以及与本需求无关的代码

【2 认字】（必须一字不差）
- 术语：<名称> = <数值> = `<枚举常量名>`；易混邻域值：<…>
- 配置 key：`<…>`；配置字段：`<字段名>`(<类型>)；注入点：`<全类名>`
- 方法签名：`<返回类型 方法名(参数)>`；接口：`<METHOD /path>`，`<注解>`，返回 `<类型>`

【3 判断顺序】（按顺序判定，命中即返回并终止）
| 步 | 条件 | 结果 | 是否继续 |
| S1 | <条件> | <结果> | 终止 |
| S2 | <条件> | <结果> | 终止 —— 不得继续判 <X> |
禁止写成：<最容易写错的那种表述>

【4 按什么入参分流】
| 入参特征 | 走哪条逻辑 | 返回几条 / 顺序 |
| <特征> | 新逻辑 | N 条 |
| <特征> | 旧逻辑 | M 条 |
复杂动作拆子行；写清收尾（遍历完统一做什么、结果追加到哪里）。
示例：入参 <具体值> → 期望 <N> 条、顺序 <…>
常见错误：<最容易做错的>，必须 <正确做法>

【5 入口与下游】
以 `类#方法` 为入口，沿调用链逐点确认下列下游是否需要同步改：
`类#a`（要改 / 不改 + 依据）、`类#b`（要改 / 不改 + 依据）
未列出的不要改。若含"判断是否已兼容"类任务：输出结论表（逐项 已兼容 / 需改 + 依据）。

【6 不许含糊 + 断言验收】
禁用：「按现有方式」「保证与之前相等」「业务规则不变」「尽量」「合理」→ 一律换成
「输入 <A> → 返回 <N> 条 / 字段 <P>=<Q>」。
断言（逐条可观测）：
1) <输入> → <期望>    2) <输入> → <期望>    3) <字段/位置> = <期望>
验收：`<可执行命令>`，预期 <…>
```


```
【2 认字】（必须一字不差）

- 术语：纯定软电单（纯零售）= **12** = `RETAIL_REGULAR_SD_RETAIL_ORDER` 易混邻域值：配套单(定软电)=4、配套单(团装)=26、配套单(局装)=34；复合类型：主材单(整装)&配套单(定软电)=−99、主材单(团装)&配套单(团装)=−98、主材单(局装)&配套单(局装)=−97 排程引用状态 = `DELIVERY_ACTIVE`

- 配置 key：`pure.retail.schedule.switch.companyList`

- 配置字段：`nationEnable`(Boolean) + `companyList`(List<String>)，**新建一个类承载**

- 注入点：`com.ke.utopia.service.apollo.ApolloConfig`

- 方法签名：`Boolean isPureRetailScheduleSwitchOn(List<String> mdmCode)` —— 传列表时要求**全部**命中

- 接口：`POST /api/config/pureRetailScheduleSwitch`，`@Valid @RequestBody List<String>`，返回 `ResultDTO<Boolean>`（全部命中才 true）；挂 `CategoryProcessFeign` + `CategoryProcessController`

- 枚举：纯零售用已有枚举值 12，**不得新造**枚举或常量

【3 判断顺序】（按顺序判定，任一步命中即返回并终止）

|   |   |   |   |
|---|---|---|---|
|步|条件|结果|是否继续|
|S1|配置为 `null`|`false`|终止|
|S2|`nationEnable == true`|**`true`**|**终止 —— 不得继续判白名单**|
|S3|白名单非空 且 传入的**全部分公司**都在白名单内|`true`|终止|
|S4|其余情况|`false`|终止|

禁止写成：「全国开关开启 **且** 白名单命中」。 （S2 是本次重点：`nationEnable == true` 表示「全国全量开通、直接放行」。）

【4 按什么入参分流】 单据类型集合：`docTypes = {documentType} ∪ documentTypeList`（合并去重，忽略 null）。**两个来源都取，不得只认列表。** 不变量：只要 `documentType` 或 `documentTypeList` 任一来源能算出单据类型，`docTypes` 就不为空；只有两个来源都取不到时才为空。**不得**因为"列表来源为空"就判定 `docTypes` 为空。 返回顺序（两个入口相反，**不要统一成一种**）：列表入口 = 新逻辑结果在前（遍历中即时追加）、旧逻辑结果整批追加在末尾；品类入口 = 旧逻辑结果在前、新逻辑结果在后。 返回条数：分源命中时一个入参会展开成多条（12 一条 + 其余每个单据类型各一条）；**不得**为"与入参一一对应"做条数回填。

**4A · 列表入口 `queryConfigOFCList`（批量入参，逐参数判定）**

|   |   |   |
|---|---|---|
|行|条件|动作|
|A0|`configFlag == null` 或 `true`|该参数整参走新逻辑。不判断开关、不判断单据类型|
|A1–A3|`configFlag == false` 且「`docTypes` 为空 / 开关未命中 / `docTypes` 不含 12」任一成立|该参数整参走旧逻辑|
|A4|`configFlag == false` 且 `docTypes` 含 12 且 开关命中|逐项拆分（见子行）|

A4 逐项拆分：遍历 `docTypes` 每一项 `dt`

|   |   |   |
|---|---|---|
|子行|条件|动作|
|A4.1|`dt == 12`|新建查询参数对象（复制原参数），把单据类型字段设为 `12`、单据类型列表设为 `[12]` → 走新逻辑 → 结果直接追加到返回值。**禁止直接把原参数传入新逻辑**|
|A4.2|`dt != 12` 且 `secondTypeList` 中能筛出「映射后 == dt」的项|用筛出的子集构造旧逻辑入参，收集待批|
|A4.3|`dt != 12` 且筛不出任何项|跳过，不查询，不记日志|

A 收尾：全部参数遍历完后，把收集到的旧逻辑入参**整批一次**调用旧查询接口，结果追加到返回值末尾。

**4B · 品类入口 `queryCategoryConfigOFCList`（单个入参）**

|   |   |   |
|---|---|---|
|行|条件|动作|
|B0|`configFlag == null` 或 `true`|整参走新逻辑，不判断开关|
|B1–B3|`configFlag == false` 且「`docTypes` 为空 / 开关未命中 / `docTypes` 不含 12」任一成立|整参走旧逻辑|
|B4|`configFlag == false` 且 `docTypes` 含 12 且 开关命中|独立查两次后拼接（见下）|

B4 两段执行：

|   |   |
|---|---|
|段|动作|
|B4.1|从 `secondTypeList` 剔除「映射后 == 12」的项，剩余入参走旧逻辑；剔完为空则跳过本段|
|B4.2|新建查询参数对象（复制原参数），单据类型收敛为只含 12 → 走新逻辑|
|B4 合并|两段结果用 `addAll` 拼成一个列表返回（旧在前、新在后）|

示例（务必对齐）：入参单据类型 `[1, 12]` 且开关命中 → B4.1 用 `1` 查旧逻辑，返回 1 条 → B4.2 用 `[12]` 查新逻辑，返回 1 条 → **最终返回 2 条**。 常见错误：命中时**只查一次新逻辑、返回 1 条**。这是错的，必须两段都查。
```

```
在 `edar-starlord-service` 中新建 `exp/guided/r2` 分支并在分支中实现「纯零售进排程」改造。本提示词为**六槽结构**，逐槽填写；槽内契约必须一字不差。

【1 改哪里】

- 改（2 个模块、6 个方法）：
    
    - 配置查询分源：`ConfigQueryServiceImpl#queryConfigOFCList`、`ConfigQueryServiceImpl#queryCategoryConfigOFCList`
    
    - 任务模式判定：`TaskTemplateRoleServiceImpl#getTaskMode`、`TaskTemplateServiceImpl#convert2NMaterialTemplateUnitParam`、`TaskTemplateServiceImpl#buildDeliveryFlowTaskTemplateDTO`、`DeliveryMaterialBizServiceImpl#createByDelivery`

- 新增：1 个承载灰度配置的类（见【2 认字】）

- 不要动：`MaterialCreateV2ServiceImpl`、`ComboInfoManager` / `ComboInfoManagerImpl`、`pom.xml`、以及任何与本需求无关的代码

【2 认字】（必须一字不差）

- 术语：纯定软电单（纯零售）= **12** = `RETAIL_REGULAR_SD_RETAIL_ORDER` 易混邻域值：配套单(定软电)=4、配套单(团装)=26、配套单(局装)=34；复合类型：主材单(整装)&配套单(定软电)=−99、主材单(团装)&配套单(团装)=−98、主材单(局装)&配套单(局装)=−97 排程引用状态 = `DELIVERY_ACTIVE`

- 配置 key：`pure.retail.schedule.switch.companyList`

- 配置字段：`nationEnable`(Boolean) + `companyList`(List<String>)，**新建一个类承载**

- 注入点：`com.ke.utopia.service.apollo.ApolloConfig`

- 方法签名：`Boolean isPureRetailScheduleSwitchOn(List<String> mdmCode)` —— 传列表时要求**全部**命中

- 接口：`POST /api/config/pureRetailScheduleSwitch`，`@Valid @RequestBody List<String>`，返回 `ResultDTO<Boolean>`（全部命中才 true）；挂 `CategoryProcessFeign` + `CategoryProcessController`

- 枚举：纯零售用已有枚举值 12，**不得新造**枚举或常量

【3 判断顺序】（按顺序判定，任一步命中即返回并终止）

|   |   |   |   |
|---|---|---|---|
|步|条件|结果|是否继续|
|S1|配置为 `null`|`false`|终止|
|S2|`nationEnable == true`|**`true`**|**终止 —— 不得继续判白名单**|
|S3|白名单非空 且 传入的**全部分公司**都在白名单内|`true`|终止|
|S4|其余情况|`false`|终止|

禁止写成：「全国开关开启 **且** 白名单命中」。 （S2 是本次重点：`nationEnable == true` 表示「全国全量开通、直接放行」。）

【4 按什么入参分流】 单据类型集合：`docTypes = {documentType} ∪ documentTypeList`（合并去重，忽略 null）。**两个来源都取，不得只认列表。** 不变量：只要 `documentType` 或 `documentTypeList` 任一来源能算出单据类型，`docTypes` 就不为空；只有两个来源都取不到时才为空。**不得**因为"列表来源为空"就判定 `docTypes` 为空。 返回顺序（两个入口相反，**不要统一成一种**）：列表入口 = 新逻辑结果在前（遍历中即时追加）、旧逻辑结果整批追加在末尾；品类入口 = 旧逻辑结果在前、新逻辑结果在后。 返回条数：分源命中时一个入参会展开成多条（12 一条 + 其余每个单据类型各一条）；**不得**为"与入参一一对应"做条数回填。

**4A · 列表入口 `queryConfigOFCList`（批量入参，逐参数判定）**

|   |   |   |
|---|---|---|
|行|条件|动作|
|A0|`configFlag == null` 或 `true`|该参数整参走新逻辑。不判断开关、不判断单据类型|
|A1–A3|`configFlag == false` 且「`docTypes` 为空 / 开关未命中 / `docTypes` 不含 12」任一成立|该参数整参走旧逻辑|
|A4|`configFlag == false` 且 `docTypes` 含 12 且 开关命中|逐项拆分（见子行）|

A4 逐项拆分：遍历 `docTypes` 每一项 `dt`

|   |   |   |
|---|---|---|
|子行|条件|动作|
|A4.1|`dt == 12`|新建查询参数对象（复制原参数），把单据类型字段设为 `12`、单据类型列表设为 `[12]` → 走新逻辑 → 结果直接追加到返回值。**禁止直接把原参数传入新逻辑**|
|A4.2|`dt != 12` 且 `secondTypeList` 中能筛出「映射后 == dt」的项|用筛出的子集构造旧逻辑入参，收集待批|
|A4.3|`dt != 12` 且筛不出任何项|跳过，不查询，不记日志|

A 收尾：全部参数遍历完后，把收集到的旧逻辑入参**整批一次**调用旧查询接口，结果追加到返回值末尾。

**4B · 品类入口 `queryCategoryConfigOFCList`（单个入参）**

|   |   |   |
|---|---|---|
|行|条件|动作|
|B0|`configFlag == null` 或 `true`|整参走新逻辑，不判断开关|
|B1–B3|`configFlag == false` 且「`docTypes` 为空 / 开关未命中 / `docTypes` 不含 12」任一成立|整参走旧逻辑|
|B4|`configFlag == false` 且 `docTypes` 含 12 且 开关命中|独立查两次后拼接（见下）|

B4 两段执行：

|   |   |
|---|---|
|段|动作|
|B4.1|从 `secondTypeList` 剔除「映射后 == 12」的项，剩余入参走旧逻辑；剔完为空则跳过本段|
|B4.2|新建查询参数对象（复制原参数），单据类型收敛为只含 12 → 走新逻辑|
|B4 合并|两段结果用 `addAll` 拼成一个列表返回（旧在前、新在后）|

示例（务必对齐）：入参单据类型 `[1, 12]` 且开关命中 → B4.1 用 `1` 查旧逻辑，返回 1 条 → B4.2 用 `[12]` 查新逻辑，返回 1 条 → **最终返回 2 条**。 常见错误：命中时**只查一次新逻辑、返回 1 条**。这是错的，必须两段都查。

**4C · 新逻辑输出规格（两入口通用）** 复合类型的结果**拆成两条**，内容相同、仅单据类型不同：

|   |   |   |
|---|---|---|
|复合类型|主材单侧|配套单侧|
|−99|保持 −99|4|
|−98|保持 −98|26|
|−97|保持 −97|34|

【5 入口与下游（含落点锚点）】 本节 4 处是**互相独立**的判定点（不是一条链上的 4 站）；**除下表 4 处外，其余调用点一律不改**（未列出的不要改）。

|   |   |   |   |   |
|---|---|---|---|---|
|#|文件#方法|触发条件|动作|落点锚点|
|T1|`TaskTemplateRoleServiceImpl#getTaskMode`|`projectBusiness == RETAIL_2_5` 且 开关命中 `[mdmCode]`|`return ModeEnum.DELIVERY_FLOW`|在该 `RETAIL_2_5` 分支内、原 `return HOME2_5_MANPOWER` **之前**|
|T2|`TaskTemplateServiceImpl#convert2NMaterialTemplateUnitParam`|`unitParam.getMdmCodes()` 非空 且（开关命中 `mdmCodes` 或 开关命中 `[mdmCode]`）且 `saleType == RETAIL`|`unitParam.setMode(ModeEnum.DELIVERY_FLOW)`|**必须在「全国配置不使用套餐 → `setProductComboIds(null)`」块之后**|
|T3|`TaskTemplateServiceImpl#buildDeliveryFlowTaskTemplateDTO`|`saleType == RETAIL`|`param.setDocumentTypeList([12])`（**覆盖默认的 `[-99]`**）|在 `param.setMdmCompanyCodeList(...)` **之前**|
|T4|`DeliveryMaterialBizServiceImpl#createByDelivery`|`projectOrderDetailBO == null` 且 `supportMode == HOME2_5_MANPOWER` 且 开关命中 `[mdmCode]`|`setSupportMode(ModeEnum.DELIVERY_FLOW)`|在原有 `materialScheduleSwitch` 判定块**之后**|

硬要求：T2 落点放早了 → 纯零售的 `productComboId` 不会被清空；T3 不做 → 排程查询仍按复合类型 `[-99]` 过滤、拿不到零售排程配置；T1/T4 的开关入参都传 `Collections.singletonList(mdmCode)`。

【6 不许含糊 + 断言验收】 禁用表述：「按现有方式」「保证与之前相等」「业务规则不变」「尽量」「合理」—— 一律换成「输入 A → 返回 N 条 / 字段 P=Q」。

硬约束（逐条遵守）：

1. 两个方法的单据类型来源必须一致：都取「单值 ∪ 列表」合并去重，不允许一个只读列表。

2. 筛不出对应 `secondType` 的类型直接跳过，**禁止为此新增任何 `log.warn` / `log.error` / 异常**。

3. 走新逻辑前必须构造新参数对象并收敛单据类型，不得把含多类型的原参数直接传入。

4. 注释必须与实际行为一致：不得留下描述"未实现行为"的注释；改动后原注释若与代码不符，一并修正。

5. 返回值契约显式：条数与顺序见【4】。

6. 不得删除已有判空守卫；不得为省事引入新的 NPE 风险（例如对可能为 `null` 的集合直接构造包装，或对可能为 `null` 的 `secondTypeList` 直接 `.stream()`）。

7. 不得越界：只改【1】【5】点名的文件与方法，不顺手重构。

8. 对外契约不变：两个查询方法的入参、返回类型不变；`configFlag` 为空 / true 分支的行为不变。

【必须新增】（这是必须实现的守卫，**不是**"既有行为"）：新逻辑的生效单据类型列表为空时 → 记告警日志并返回空结果，不抛异常、不降级。 注意：**不要**把它扩大成"单值入参也返回空"的捷径 —— 单值来源能算出单据类型时不算空。

断言（逐条可观测，交付前逐条自证）：

1. 开关：配置 `{"nationEnable":true}` 时 `isPureRetailScheduleSwitchOn([任意分公司])` → `true`（不看白名单）；

2. 分源条数：`configFlag == false`、入参单据类型 `[1, 12]`、开关命中 → 返回 **2 条**；

3. 顺序：`queryConfigOFCList` 新逻辑结果在前；`queryCategoryConfigOFCList` 旧逻辑结果在前；

4. 落点：T2 的灰度块在 `setProductComboIds(null)` 之后；T3 的 `documentTypeList` 为 `[12]`；T1/T4 的开关入参为 `Collections.singletonList(mdmCode)`；

5. 未越界：改动 diff 中不出现 `MaterialCreateV2ServiceImpl`、`ComboInfoManager` / `ComboInfoManagerImpl`、`pom.xml`；

6. 编译：`./mvnw -o compile -pl edar-starlord-service -am -Dmaven.test.skip=true` 通过。
```