1. **`submit` 是幂等的**（`MeasureFormTemplateServiceImpl:74-78`）：先按 `(taskDispatchId, taskDispatchNodeId)` 查 detail，已存在就 `return true` — **不覆盖**。所以同一节点第二次提交（包括"可以复尺"后再"无法复尺"）不会重新落库。com.ke.utopia.service.impl.MeasureFormTemplateServiceImpl#submit
   List<MeasureMaterialDetail> measureMaterialDetails =  measureFormTemplateDao.queryMeasureMaterial(measureFormSubmitParam.getTaskDispatchId(), measureFormSubmitParam.getTaskDispatchNodeId());  有无复尺数据
   
   点击“可以复尺”后提交：  
- 调用 saveMeasure  
- 接口：POST /starlord/manpower/measure/task/saveMaterialMeasure  
- 同时提交测量明细 unitList、图片 imageList 等数据。  
  
点击“无法复尺”后提交：  
- 调用 notifySubmit  
- 接口：POST /starlord/manpower/measure/task/handle  
- 同时提交无法复尺说明 remarks、无法复尺原因 remarksFilter（新开关开启时）和图片 images 等数据。

getMaterialMeasure

http://life-fe-luban-node.offline-beta.ttb.test.ke.com/scmnorth-proxy/edar-starlord/manpower/measure/task/getMeasureTaskInfo

s/starlord/manpower/measure/task/handle


## 一、明细回显（真正返回 detail + unit 的数据）

| 接口                                                            | 入口方法                                                                           | 特点                                                      |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------- |
| `POST /api/material-measure/get-task-measure`                 | `MaterialMeasureController#getMaterialMeasureInfo`                             | 本节点明细（未复尺视图）                                            |
| `POST /api/material-measure/get-task-measure/last-measure`    | `#getLastMeasureInfo` → `MaterialMeasureServiceImpl#getLastMeasureInfo`(L2047) | **只找上次"测量"任务**（taskType=MEASURE），限复尺节点且未完成              |
| `POST /manpower/task/getLastMeasureInfoV2`                    | `MaterialMeasureTaskController#getLastMeasureInfoV2`(L345)                     | 找上次测量**或**复尺任务，支持按 nodeId 或 purchaseOrderNo；供"复用上次复尺数据" |
| `GET /manpower/measure/task/queryTaskDetail`                  | `#queryTaskDetail`(L128)                                                       | 本节点明细，入参 nodeId（传任务 id 会自动换算）                           |
| `GET /manpower/measure/task/queryTaskDetailByPurchaseOrderNo` | `#queryTaskDetailByPurchaseOrderNo`(L138)                                      | 同上，入参订单号（纯数字则当 nodeId）                                  |
| `POST /measure/form/get-v2`                                   | `MeasureFormTemplateController#measureDetailV2`                                | 与 queryTaskDetail 同一个 `getMaterialMeasureData(nodeId)`  |
| `POST /measure/form/get`                                      | `#measureDetail`                                                               | **只返回 detail+unit 本身**，不拼项目/模板信息                        |
| `GET /manpower/measure/task/getMeasureTaskInfo` †             | `#getMeasureTaskInfo`(L167)                                                    | 见下组说明                                                   |
## 二、只回显「按钮 / 时间 / 提交记录」（读 detail，但不返明细）

| 接口                                              | 入口                                                                                                          |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `GET /manpower/measure/task/getMeasureTaskInfo` | `MaterialMeasureTaskController#getMeasureTaskInfo` → `getMaterialMeasure(nodeId)`                           |
| `GET /api/material-measure/v2/get-task`         | `MaterialMeasureController#getMaterialMeasureV2` → `getMaterialMeasure(projectOrderId, categoryId, nodeId)` |

**骨架用 `getMeasureTaskInfo`/`v2/get-task`，本节点回填用 `queryTaskDetail`**




梳理PO单和test node汇总的。
给自域，什么情况下会填充 test node 是多少    测量复尺都会
需要新增一个转接接口需要由 PO 单转为 Test Node ID、Test Dispatch ID 和品类 ID。  /manpower/task/queryTaskDetail

GET /scm-proxy/edar-starlord/api/material-measure/v2/get-task
POST .../api/material-measure/get-task-measure/last-measure
GET .../manpower/task/queryNodeCanRestart-vss同-有
POST .../material-measure/interface/config/query/requirement-vss同--有
POST /scm-proxy/edar-starlord/material-task/dispatch/material-task-restart-lis  -you
POST /gateWay-proxy/utopia-scs-openapi/construction/site/getSiteLiveInfo -有






















