# 通知复尺自动化 — 开发总结（含造数 / 踩坑 / 反复修改）

> 分支：`feat-20260817-remeasureNotificationAutomation` ｜ 完成日期：2026-08-20
> 本文档是本次开发的**收口文档**：覆盖全部工作与改动、造数方案演进、踩坑点、反复修改的地方，供复盘与交接使用。

---

## 1. 需求背景

复尺（recheck）完成后，系统识别出「需要再次上门」的任务，生成**通知任务**：工长在通知页面确认期望上门时间与图片，系统按确认结果执行通知动作（改状态、激活下一节点、发推送）。本次开发把这条链路从「人工逐条操作」升级为「识别 → 填充 → 确认 → 自动/批量执行」的自动化闭环，并补齐通知页面所需的数据查询与保存能力。

---

## 2. 交付接口全景（7 个接口）

| # | 接口 | 方向 | 职责 | 关键实现点 |
|---|------|------|------|-----------|
| 1 | `POST /api/recognize/task/result` | 外部→本系统 | 识别结果接入，写入 `material_notify_task_automation` | 8 字段全必填；images 逗号拼接；`notify_execute_type/ai_adoption_type` 固定 0；judge 由 remarks 中的 `\|Jx` 决定；日期 yyyy-MM-dd |
| 2 | `POST /api/material/notify/task/save` | 通知页→本系统 | 手动保存 images + noticeRetainTime 到 `task_dispatch_node` | 仅写 node 两列，不触发 handleNode 全链路；node 不存在/为空抛 `ERROR_PARAM_ILLEGAL` |
| 3 | `POST /api/material/notify/task/auto-fill` | 通知页→本系统 | 页面内容自动填充 | node 优先、automation 兜底（仅 node 缺图或缺时间才查昨日判定行）；Apollo 开关 A 未命中返回空 VO（fail-closed） |
| 4 | `POST /api/material/notify/task/batch/query` | 通知页→本系统 | 按项目分组批量查询 | judge=1→notifyTasks、judge=2/3/无记录→delayTasks（按 `estimated_time<=now+3d` 过滤）、judge=4→丢弃；组内按 estimatedTime 升序（越近今日越靠前） |
| 5 | `POST /api/material/notify/task/batch/handle` | 通知页→本系统 | 批量执行通知 | 逐节点 items 明细（每项独立 images/时间）；并行调 handleNode 全链路；单件异常不阻塞整批；返回 `{successCount, alreadyHandledCount}` |
| 6 | `POST /api/material/notify/task/auto/execute` | 定时/系统→本系统 | 每日自动执行通知 | 扫描 automation `judge_result=1 + notify_execute_type=0 + task_type=3 + recognize_time=昨日零点`；CAS 门禁写 `notify_execute_type=2`；按 (projectOrderId, materialCode) 反查 node，restart 去重；返回 `{candidateCount, executedCount, skippedCount, failedCount}` |
| 7 | `POST /api/material/notify/task/site/complete/query` | 通知页→本系统 | 现场已完成（第三桶）查询 | judge=4 桶，复用 batch/query 管线，受开关 A 过滤，无 summary 时间窗限制 |

**链路关系**：识别结果 → `/result` 落库（接口 1）→ 通知页 `batch/query`（4）展示 → `auto-fill`（3）预填 / `save`（2）手动改 → `batch/handle`（5）或 `auto/execute`（6）执行 → 已完成看 `site/complete/query`（7）。

**配套功能**：
- `summary` 汇总接口（notify/delay 两桶计数，仅 09:10-10:00 / 13:00-14:00 / 19:00-21:00 时间窗内统计）。
- Apollo 开关两枚（均 fail-closed，默认 `{}` 全关）：
  - `notification.remeasure.automation.open`（开关 A，功能级）：城市/工长/工程部 三集 OR。
  - `notification.remeasure.automation.autoNotify`（开关 B，品类级）：`auto/execute` 按 material_code 三集 OR。

---

## 3. 代码改动清单（git log 实证，24 笔提交，08-17 ~ 08-20）

### 3.1 完整提交时间线

| # | 提交 | 时间 | 内容 | 阶段 |
|---|------|------|------|------|
| 1 | `355ea9ac0` | 08-17 20:44 | 数据库 mapper（Model/Example/Mapper/XML 生成物） | 一期：建表 DAO |
| 2 | `e74c41e8b` | 08-17 21:20 | 上游识别结果批量落库（一期主体） | 一期：接收落库 |
| 3 | `00a1515c9` | 08-17 21:24 | 上游识别结果批量落库（补充提交） | 一期：接收落库 |
| 4 | `9411268aa` | 08-18 14:43 | 查询可通知/延期任务列表接口，先取 Node（batch/query 主体） | 二期：batch/query |
| 5 | `b60033d0d` | 08-18 15:41 | 汇总接口 + 修改查询昨日识别结果（summary + T+1 昨日口径） | 二期：summary |
| 6 | `4bf7c13ef` | 08-18 15:46 | 列表接口加 taskDispatchId、businessId | 二期：batch/query 增强 |
| 7 | `c5223a58e` | 08-18 16:35 | 列表接口加 taskNodeName、estimatedTime、nodeType | 二期：batch/query 增强 |
| 8 | `6f0b1639c` | 08-18 17:18 | Apollo 两个开关（NotifyAutomationConfig） | 二期：Apollo |
| 9 | `ac2d8a725` | 08-18 17:26 | 个数查询接口接入 Apollo | 二期：summary 接开关 |
| 10 | `2b49c34ba` | 08-18 17:27 | 个数查询接口接入 Apollo（补充） | 二期：summary 接开关 |
| 11 | `14bed571a` | 08-18 21:26 | 一键通知复尺批量执行（batch/handle 首版） | 二期：batch/handle |
| 12 | `245e64a80` | 08-18 22:07 | 通知页面内容填充接口（auto-fill） | 二期：auto-fill |
| 13 | `913bda942` | 08-19 10:28 | 通知页面修改暂存图片和时间接口（save） | 二期：save |
| 14 | `11485afb5` | 08-19 11:24 | 自动落库手动校验（/result validateParams 手写） | 一期：校验改造 |
| 15 | `78752bc87` | 08-19 11:54 | 自动通知接口（auto/execute 首版） | 二期：auto/execute |
| 16 | `142e6981d` | 08-19 14:39 | 打包（pom 调整） | 构建 |
| 17 | `d46770e72` | 08-19 16:21 | 结果接收入参校验（images 空元素剔除） | 一期：校验改造 |
| 18 | `1c5d04d6c` | 08-19 20:56 | 一键通知接口入参修改（items 明细 + NotifyNodeHandleItem） | 二期：batch/handle 改造 |
| 19 | `a01db8273` | 08-19 21:46 | 一键通知接口修改（批量执行 + VO） | 二期：batch/handle 改造 |
| 20 | `e789b1d0b` | 08-19 21:52 | 自动通知日志增加 | 二期：auto/execute 日志 |
| 21 | `319f5ef65` | 08-19 21:52 | 自动通知日志增加 | 二期：auto/execute 日志 |
| 22 | `b1d3491df` | 08-19 21:57 | 自动通知日志增加 | 二期：auto/execute 日志 |
| 23 | `916eab359` | 08-20 10:44 | 查询现场已完成的任务接口（site/complete/query） | 二期：site/complete |
| 24 | `3a1aa6aaa` | 08-20 14:11 | 自动通知接口实现（auto/execute 最终版，CAS 修正） | 二期：auto/execute 定版 |

> **早期废弃代码**：`NotifyTaskTypeUtil.java`（task_type 5 位拼接串拆/拼工具类，String 口径时期创建，08-18 Integer 口径重建后废弃，从未入 git——孤儿提交 `7b3c46d69` 的 `NotifyTaskBatchQueryServiceImpl:64` import 它，检出该提交会编译失败）。

**新增/修改的类清单（最终版）**：
- api 模块：`NotifyBatchHandleParam`、`NotifyNodeHandleItem`、`NotifyTaskQueryParam`、`NotifyTaskResultParam`、`NotifyTaskAutoFillParam`（DTO）；`NotifyTaskAutoExecuteResultVO`、`NotifyTaskAutoFillVO`、`NotifyTaskBatchHandleResultVO`、`NotifyTaskBatchHandleGroupVO/ItemVO`、`NotifyTaskBatchQueryVO`、`NotifyTaskDetailVO`、`NotifyTaskProjectGroupVO`、`NotifyTaskSummaryVO`、`NotifyHandleProjectGroupVO`、`NotifyHandleTaskItemVO`（VO）。
- dao 模块：`MaterialNotifyTaskAutomationDao(+Impl)`、`MaterialNotifyTaskAutomationMapper(+xml)`——新增 `selectAutoNotifyCandidates` 候选扫描 SQL，修正 `updateNotifyExecuteWithCas` CAS SQL。
- service 模块：`NotifyTaskBatchQueryService(+Impl)`（batch/query、summary、save、auto-fill、batch/handle、auto/execute、site/complete/query 全链路）、`NotifyTaskAutomationServiceImpl`（/result 接入 + validateParams 手写校验 + filterImages 剔除）。
- web 模块：`NotifyTaskBatchQueryController`、`NotifyTaskAutomationController`。

---

## 4. 造数方案演进（重点）

### 4.1 演进链条

```
notify_task_automation_test_data.sql（v0）
  → 直插 automation 表，依赖测试库已有真实 node —— 只能测查询链路
  → 被 seed.sql 取代

notify_task_automation_seed.sql（v1）
  → 自造 task_dispatch + task_dispatch_node + automation，不依赖真实节点
  → 但用了 autotest_dispatch/autotest_node TEMPORARY TABLE
  → 用户反馈「没有 autotest 的表」：临时表跨 session 失效 → 废弃

notify_automation_seed_v2.sql（v2，最终）
  → 纯 INSERT，零建表、零临时表（grep 验证零命中），头尾自带清理可重跑
  → automation 13 行经 /result 接口造（SQL 末尾 SELECT 生成 payload）
  → 订单号迭代 3 轮后定版（见 4.3）
```

### 4.2 最终交付物（doc/ 下）

| 文件 | 作用 |
|------|------|
| `doc/notify_automation_seed_v2.sql` | 造数：task_dispatch + task_dispatch_node（6 主节点[含 M05 restart 0/1 去重用例] + 5 次节点）+ automation（经 /result 接口落库） |
| `doc/notify_automation_execute_plan.md` | 执行方案：环境 / Apollo 开关 JSON / 接口契约速查 / 场景矩阵 / 预期断言 / DB 校验 SQL / 清理 / 风险 |
| `doc/test_notify_interfaces.sh` | curl 自动化脚本（曾交付，**后被用户删除**，改为手工 curl 逐接口验证） |

### 4.3 造数关键决策与迭代（踩过的反复点）

1. **订单号三换**：自造订单号 → 用户提供 16 个真实订单号（本机 macOS Vision OCR 提取，取前 4 个 `826072010000001909` 等）→ 用户先后两次换号 → **最终定版 3 个**：`546068027139760128`（proj 9100000001，M01/M02）、`577257627351097344`（proj 9100000002，M03/M04）、`499472368956997632`（proj 9100000004，M05）。
2. **规模精简**：15 行 → 5 用例 / 4 订单 → 5 用例 / 3 订单。仅 `order_no`/`home_order_no` 用真实号，`project_order_id` 自造（假项目 → RPC 取不到真实订单 → 开关只能走 projectManagers 维度，方案已注明）。
3. **automation 判定口径**：**当天零点 → 昨日零点（T+1）**，查询用 `DATE_SUB(CURDATE(), INTERVAL 1 DAY)` 精确等值（recognize_time 是 DATE 列，非范围）。
4. **judge 分支全覆盖**：M01=1 / M02=2 / M03=3 / M04=4 / M05=1，覆盖 4 种 judge + 延期窗口命中/过滤 + autoNotify 命中(M05)/跳过(M01) + 重启去重 + 负向用例。
5. **预期断言**：batch/query notify=2/delay=2 + 双 projectOrderId 过滤；auto/execute 首次 2/1/1/0、复测 0/0/0/0；handle(M01) success=1 + 幂等 already=1。

### 4.4 最终版三表布局（定版数据）

> **注意**：用户后续要求把临时表方式改为直接 INSERT，最终 SQL 仍在迭代中。以下为 v2 版本的布局快照。

**task_dispatch（16 行）**：

| 用例 | material_code | project_order_id | order_no/home_order_no | task_type | process_status | current_node_type | 场景 |
|------|---------------|------------------|------------------------|-----------|----------------|---------------------|------|
| M01 | AUTOTEST_M01 | 9100000001 | 546068027139760128 | 3 | 2 | 20 | judge=1 可通知 |
| M02 | AUTOTEST_M02 | 9100000001 | 546068027139760128 | 3 | 2 | 20 | judge=2 不可通知（同项目验证分组） |
| M03 | AUTOTEST_M03 | 9100000002 | 577257627351097344 | 3 | 2 | 20 | judge=3 无法判断 |
| M04 | AUTOTEST_M04 | 9100000002 | 577257627351097344 | 3 | 2 | 20 | judge=4 现场已完成（不返回） |
| M05 | AUTOTEST_M05 | 9100000004 | 499472368956997632 | 3 | 2 | 20 | judge=1 + autoNotify 命中（auto/execute 目标） |
| M06 | AUTOTEST_M06 | 9100000001 | 546068027139760128 | 3 | 2 | 20 | judge=1 + execute_type=1（CAS 防重复） |
| M07 | AUTOTEST_M07 | 9100000002 | 577257627351097344 | 3 | 2 | 20 | judge=1 + autoNotify 未配（跳过） |
| M08 | AUTOTEST_M08 | 9100000001 | 546068027139760128 | 3 | 2 | 20 | autoNotify 命中 |
| M09 | AUTOTEST_M09 | 9100000002 | 577257627351097344 | 3 | 2 | 20 | autoNotify 未配（验证跳过） |
| M10×2 | AUTOTEST_M10 | 9100000004 | 499472368956997632 | 3 | 2 | 20 | restart=0（丢弃）+ restart=1（保留）验证去重 |
| M11 | AUTOTEST_M11 | 9100000001 | 546068027139760128 | 3 | 2 | **60** | 负向：node_type≠20 |
| M12 | AUTOTEST_M12 | 9100000002 | 577257627351097344 | 3 | **4** | 20 | 负向：process_status≠2 |
| M13 | AUTOTEST_M13 | 9100000004 | 499472368956997632 | 3 | 2 | 20 | 负向：executor≠工长 |
| M20 | AUTOTEST_M20 | 9100000001 | 546068027139760128 | 3 | 2 | 20 | 延期窗口命中（estimated+1d） |
| M21 | AUTOTEST_M21 | 9100000001 | 546068027139760128 | 3 | 2 | 20 | 延期窗口命中（同项目验证分组） |

**task_dispatch_node（15 行）**：6 主节点（M05 restart=0/1 两行验证去重）+ 5 次节点（node_type=60），node_type=20/process_status=2/executor=88888888（测试工长A），M13 用 99999999（工长B，负向）。estimated_time 覆盖 +1d/+2d/+3d/+5d/+10d。M02/M06 有图片（验证 auto-fill node 优先），M01/M07 无图片（验证 automation 兜底）。

**material_notify_task_automation（15 行，经 /result 接口落库）**：judge_result 覆盖 1/2/3/4 全枚举；recognize_time=昨日；recommend_visit_time=T+3。落库后需手动补一条 SQL 将 M06 的 notify_execute_type 改为 1（验证 CAS 防重复）。

---

## 5. 踩坑点汇总

### 5.1 校验类（最大的一类坑）

| # | 坑 | 结论/解法 |
|---|----|----------|
| 1 | **`@Valid @RequestBody List<X>` 不校验元素**：Hibernate Validator 只对「bean 字段上的 @Valid」级联，根对象是 List/数组时 `validator.validate()` 直接不触发元素约束（HV 6.0.18 本地对照实验实测 violations=0） | 批量接口两选一：① Wrapper 包一层（改变 HTTP 契约）；② service 手动 `validator.validate()` 循环；③ Controller 类标 `@Validated` + `@NotEmpty @Valid @RequestBody List<X>`（method validation 会级联容器元素，本项目最终选这个） |
| 2 | **Feign 接口上的 @Valid 注解不继承**，Controller 实现方法必须显式写 | Controller 逐接口补 `@Valid @RequestBody` |
| 3 | **`[""]` 能绕过 `@NotEmpty`**：非空集合但元素为空串 | 校验不能只靠集合注解，需逐元素 isBlank 检查 |
| 4 | **List 元素上的注解不级联**（items 列表里每项的 @NotNull 不生效） | items 手写循环校验每项 nodeId 非空 |
| 5 | **删 `@NotEmpty` 后旧消费方失去保护**：notify 接口改走 items 后，顶层 `taskDispatchNodeIds` 的 @NotEmpty 与契约冲突必须删 | 两处旧消费方（TaskDispatchController.batchHandle、MaterialTaskInstallerTaskServiceImpl）补手动非空校验保持等价 |

### 5.2 数据/字段类

| # | 坑 | 结论/解法 |
|---|----|----------|
| 6 | `task_dispatch_node` 实际表**无 `source` 列**（model 有但 DB 无） | INSERT 列清单去掉 source，否则 unknown column |
| 7 | `material_notify_task_automation` 表**无 state 列** | SELECT 不能写 state（方案文档已规避） |
| 8 | `recognize_time` 是 **DATE 列，精确等值匹配**（= 昨日零点），非范围 | 造数用 `DATE_SUB(CURDATE(), INTERVAL 1 DAY)`；历史数据查不到 |
| 9 | CAS SQL `notify_execute_type in (0, null)` 语义误导 | 该列永不为 null，`in(0,null)` 等价 `=0` → 修正为 `=0`；且 CAS SQL 无条件写 `ai_adoption_type`，必须显式赋 `=1` |
| 10 | TEMPORARY TABLE **跨 session 失效** | 分开 session 执行报表不存在 → v2 纯 INSERT 零建表 |
| 11 | **无全局 Jackson date-format**：Date 字段序列化行为因字段注解而异 | handle/save 的 Date 字段带 `@JsonFormat(pattern="yyyy-MM-dd HH:mm:ss", timezone="GMT+8")` → 传**字符串日期**与注解对齐 |
| 12 | **@JsonProperty 下划线字段名**：`NotifyTaskQueryParam` 的 task_type/project_order_id 是 snake_case | 此前 curl 用驼峰 taskType/projectOrderId 字段被忽略（taskType 落 null 默认 3 侥幸可用，projectOrderId 过滤完全失效） |
| 13 | Spring 的 `StringUtils` 只有 `isEmpty` 无 `isNotEmpty` | 用 `!StringUtils.isEmpty(...)` |

### 5.3 构建/启动类

| # | 坑 | 结论/解法 |
|---|----|----------|
| 14 | 编译 `-pl api,dao,service,web` 报 reactor 找不到 | 实际模块名是 `edar-starlord-api` 等**全名** |
| 15 | `-am` 编译 manager 报 `MaterialTaskRouteInstance` 找不到（api 刚编译类的时序抖动） | 重跑一次 BUILD SUCCESS |
| 16 | 启动报 `ArrayIndexOutOfBoundsException: 0`，三方库 treasurechest `CommonEnvironmentPostProcessor:39` 取 `getActiveProfiles()[0]` 越界 | 保证启动带 active profile（IDEA 运行配置 `Active Profiles=local`）；根治需改三方库源码 |
| 17 | `hotdeploy/mapper` 存在运行时 mapper 副本 | 改 Mapper.xml 时需镜像同步，否则线上/热部署加载旧 SQL |

### 5.4 业务/测试类

| # | 坑 | 结论/解法 |
|---|----|----------|
| 18 | summary 仅 **09:10-10:00 / 13:00-14:00 / 19:00-21:00** 时间窗内统计 | 非窗口时间断言 summary 为空是预期行为 |
| 19 | batch/handle 与 auto/execute 会**真实调 handleNode**（Kafka/ES/下游通知副作用） | 最后执行或打桩；执行顺序：save → handle → auto/execute |
| 20 | Apollo 开关 fail-closed 默认 `{}` | 不配置则列表/填充/保存全走空/跳过，测试需先配开关 |
| 21 | `executor_id` 必须是**登录工长 ucid**（node 维度匹配） | 造数 executor 取登录上下文一致的工长 |

---

## 6. 反复修改的地方（按时间序）

| # | 事项 | 反复过程 | 最终结论 |
|---|------|---------|---------|
| 1 | **/result 校验方案** | ① 发现 `@Valid List` 不生效 → ② 改 service 手动 `validator.validate()` 循环 → ③ 用户改选纯注解方案 `@Validated` + `@NotEmpty @Valid`（Controller）→ ④ 用户又要求「不用 validator 直接手写」→ validateParams 8 字段手写（数值 `==null`、字符串 isBlank、日期 isBlank+DateUtil.parse 格式聚合一次返回） | 手写校验 + 日期格式聚合；错误字段名统一 wire 下划线（project_order_id/material_code/images） |
| 2 | **images 空元素策略** | 最初要求 `[""]` 报错 → **决策反转**：`["", "url"]` 不报错，**剔除空元素**入库 url；仅过滤后为空才报错 | `filterImages`：逐元素 isBlank 剔除+trim+回写 `param.setImages(...)` |
| 3 | **造数临时表** | v1 用 TEMPORARY TABLE → 用户反馈「没有 autotest 的表」（跨 session 失效） | v2 纯 INSERT 零建表零临时表 |
| 4 | **订单号** | 自造 → OCR 真实订单前 4 个（8260 系列）→ 换号 → 再换号 | 最终 3 个（5460/5772/4994 系列），5 用例 |
| 5 | **automation 判定口径** | 当天零点 → **昨日零点（T+1）** | `selectYesterdayByProjectAndMaterial`，造数 `DATE_SUB(CURDATE(), INTERVAL 1 DAY)` |
| 6 | **batch/handle 入参契约** | ① 扁平 `BatchDispatchHandleParam{taskDispatchNodeIds, images, noticeRetainTime}`（BeanUtils.copyProperties 把同一份 images/时间套到所有节点，**无法区分归属**）→ ② 新建 items 列表字段 `NotifyTaskHandleItemParam{taskDispatchNodeId, images, noticeRetainTime}`，notify 只走 items → ③ 最终提交类名定为 **`NotifyNodeHandleItem`**（配合 `NotifyBatchHandleParam`），更贴近项目既有命名 | items 逐节点明细；顶层旧字段不再作为 notify 执行依据；`@NotEmpty` 删除 + 旧消费方补手动校验 |
| 7 | **sortTasks 排序方向** | ① 原 `comparing(materialCode).thenComparing(estimatedTime)`（品类优先）→ 用户指出「排反了，越靠近今日越靠前」→ ② 改为 `comparing(estimatedTime).thenComparing(materialCode)` → ③ 用户说「没区别」→ ④ 又说「我看错了不用改」 | 已落盘新版本：**考核时间升序为主键（超期负差在前），品类为次键** |
| 8 | **日期传输格式** | 反编译结论「毫秒时间戳最稳」→ 逐字段核实发现 handle/save 的 Date 带 `@JsonFormat` | **字符串日期**（`yyyy-MM-dd HH:mm:ss`）与注解对齐 |
| 9 | **JSON 字段命名** | 一直用驼峰 taskType/projectOrderId | 核实 `@JsonProperty("task_type")` → **snake_case**，方案文档契约表同步修正 |
| 10 | **CAS SQL** | `in (0, null)` → `= 0`（用户确认该列永不为 null） | 同步惠及一键通知 batch 路径 |
| 11 | **工程部获取路径** | 调研 4 条路径（订单维度 projectDepartmentCode / 组织祖先链 / 北京按工长反查 / 班组→管理员） | auto-execute 用订单维度路径①，**无需改动**（只读调研） |
| 12 | **自动化测试脚本** | `test_notify_interfaces.sh` 交付后被用户**删除** | 改手工 curl 逐接口验证 |
| 13 | **task_type 口径三轮反复** | ① 一期最初 Integer（`3`=复尺）→ ② 08-16 改为 **String 5 位拼接串**（`task_dispatch.task_type`2位 + 分隔`0` + `node_type`2位，如 `"03020"`），DB 列 INT→VARCHAR(16)，新建 `NotifyTaskTypeUtil` 拆/拼工具类 → ③ 08-18 **推翻回 Integer**：拼接串方案废弃，DB 列回 INT，`NotifyTaskTypeUtil` 废弃不提交，孤儿提交 `7b3c46d69`（String 版 batch/query）整体丢弃重写 | **Integer 直存不解析**（`3`=复尺/`11`=通知安装），DTO/Model/Mapper XML 全链路 Integer |
| 14 | **NotifyTaskTypeUtil 创建后废弃** | 08-16 String 口径时期创建（`parse("03020")→(3,20)` / `pack(3,20)→"03020"`）→ 08-18 Integer 口径重建后**从未入 git**（`??` 未跟踪状态）→ `7b3c46d69` 的 `NotifyTaskBatchQueryServiceImpl:64` import 它 → 检出该提交**编译失败** | 文件废弃，不补提交；Integer 口径下 task_type 直接用，无需拆拼 |
| 15 | **automation 查询方法改造** | ① `selectLatestBatch`（项目 IN × 品类 IN 笛卡尔范围查询 + `gmt_create desc` 内存去重取每组最新）→ ② 范围查询会扫到非目标项目/品类的判定记录 → ③ 08-16 改为 **`selectTodayByProjectAndMaterial`**（精确维度 `(project_order_id, material_code)` 行构造 IN + `task_type` + `recognize_time=当天零点`，无 order by）→ ④ 08-18 recognize_time 口径从「当天」改为「**昨日**（T+1）」，方法名随之调整为 `selectYesterdayByProjectAndMaterial` | 精确维度查询 + 昨日零点等值；`MaterialNotifyTaskAutomationQueryCondition`（`Dimension(projectOrderId, materialCode)` + `taskType` + `recognizeTime`） |

---

## 7. 遗留事项与风险

1. **接口 5/6 触发真实下游副作用**（Kafka/ES/推送）：测试时注意顺序，避免污染线上数据。
2. **site/complete/query 无 summary 时间窗**：与 summary 口径不同，联调时别按 summary 预期断言。
3. **`@Valid @RequestBody List<X>` 失效属项目隐性误区**：项目内 15+ 处同类写法均未真正校验，本次仅修了 notify 相关链路；其余接口如有批量校验诉求需按第 5.1 节方案处理（已沉淀知识库：`/Users/mirror/wiki/20.知识沉淀/📚 批量List接口@Valid校验失效排查与解决方案.md`）。
4. **treasurechest 启动 AIOOBE**：当前靠「启动必带 active profile」规避，根治需改三方库源码或注入默认 profile 的 EnvironmentPostProcessor。
5. **开关 fail-closed**：生产环境上线前必须配置两枚 Apollo 开关，否则功能静默全关（列表空、自动执行全跳过）。

---

## 8. 关键文件索引

| 类型 | 位置 |
|------|------|
| 执行方案 | `doc/notify_automation_execute_plan.md` |
| 造数 SQL（最终） | `doc/notify_automation_seed_v2.sql` |
| 旧造数（v0/v1，已废弃） | `doc/notify_task_automation_test_data.sql`、`doc/notify_task_automation_seed.sql` |
| 接口测试脚本（/result） | `doc/test_recognize_task_result.sh` |
| 需求/技术文档 | `/Users/mirror/wiki/00.需求/通知复尺自动化/`（需求背景、执行方案、作业层技术方案、迭代最终汇总） |
| 知识沉淀 | `/Users/mirror/wiki/20.知识沉淀/📚 批量List接口@Valid校验失效排查与解决方案.md` |
