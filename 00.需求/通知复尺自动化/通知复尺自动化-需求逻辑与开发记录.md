# 通知复尺自动化 — 需求逻辑与开发记录

> 文档定位：需求逻辑 + 已落地改动的**汇总整理**，衔接下面三份详档：
> - 原始需求：`需求背景.md`
> - 上游交互技术方案：`通知复尺自动化-作业层技术方案.md`
> - 实现方案（一期/二期）：`执行方案.md`
>
> 落点工程：**edar-starlord（主材任务系统）**，分支 `feat-20260814-remeasureNotificationAutomation`。
> 本文件记录 2026-08-14 ~ 08-16 期间的需求梳理、方案定稿与实际改码。

---

## 1. 需求概述

**背景**：项目经理/工长名下存在多个「通知类任务」（本次聚焦**通知复尺**），处理繁琐、影响效率。本次通过工地**摄像头**与工人拍摄的**照片**判断现场交界面是否满足复尺通知的执行标准，满足则自动判断/自动填充，减少人工操作。

**整体链路**：

```
摄像头采集 / 工人拍照
  → 上游分析（调度层）判断是否满足可通知标准
  → 判断结果通知主材任务（POST /api/deduct/task/result）
  → 落新表 material_notify_task_automation（只新增不更新，一期仅暂存）
  → 小师傅/首页：批量查询判断结果 → 一键通知批量执行
  → push 消息（09:10/13:00/19:00 悬浮框推送）
```

**一期范围**：摄像头+照片判断是否满足通知标准；满足则自动填充表单（图片 + T+3 期望上门时间），由人工提交；开城开关 `notification.remeasure.automation.open-city-engineering-forcemanucid-dept`（城市-工程部-品类-工长级联）。

**配套功能**：小师傅触达（首页/项目详情/测量复尺列表）、首页限时任务、push 消息。需要「批量查询判断结果」+「批量执行通知任务」两个接口。

---

## 2. 核心业务逻辑（已定稿）

### 2.1 判定结果枚举（我方定义，上游直传枚举值）

| 值 | 含义 | 业务去向 |
| --- | --- | --- |
| `1` CAN_NOTIFY | 可通知 | 归入 `notifyTasks` |
| `2` CANNOT_NOTIFY | 不可通知 | 归入 `delayTasks`（延期预警） |
| `3` CANNOT_JUDGE | 无法判断 | 归入 `delayTasks`（延期预警） |
| `4` SITE_COMPLETED | 现场已完成需关闭 | 本期不返回，另接口处理 |

### 2.2 task_type 拼接串（重要口径，曾推翻 Integer 设计）

上游 `task_type` 是 **5 位拼接串**：`task_dispatch.task_type`(2位，左补0) + 分隔位 `0` + `task_dispatch_node.node_type`(2位，左补0)。

- 示例：`"03020"` = 任务3(复尺) + 节点20(通知可启动)；`"09020"` = 任务9(预埋件安装) + 节点20。
- **落库零解析**：接收接口照存上游原值字符串（`VARCHAR(16)`）。
- **拆/拼只在查询入口**，收敛到 `NotifyTaskTypeUtil` 单点：`parse("03020")→(3,20)`、`pack(3,20)→"03020"`。
- 边界：task_type 仅 1-2 位适用（3 位变更/撤销单不适用）；node_type 左补 0 至 2 位。

### 2.3 T+3 计算

`recommend_visit_time = receive_time + 3 天`；`receive_time`（上游发识别结果日期）为 T 的时间基准，格式 `yyyy-MM-dd`。

### 2.4 任务匹配链路（上游不传 task_dispatch_id）

按 项目+品类+任务类型 反查，两跳定位到具体 node：

```
① task_dispatch    : project_order_id + material_code + task_type=3(复尺) + process_status=2 → task_dispatch_id（取最新）
② task_dispatch_node: task_dispatch_id + node_type=20(通知可启动) + process_status=2 + state=1 → node_id（取最新）
```

### 2.5 工长维度查询（automation 为判定源，node 表为主体）

- automation 表**无 foreman_id**：先靠 node 表 `executor_id=工长 ucid` 收敛工地集合；
- 查询以 `task_dispatch_node`「激活未完成」任务为主体，automation **仅补充判定字段**（judge/remark/图片/T+3 时间）。
- automation 关联键 = `(project_order_id, material_code, task_type原值)` + `receive_time=当天`（精确维度，见 2.6）。

### 2.6 归组规则

| 通知节点状态 | automation 判定 | 归组 |
| --- | --- | --- |
| 已完成/未激活/已取消 | 不区分 | 不处理 |
| 激活未完成 | judge=1 | `notifyTasks` |
| 激活未完成 | judge=2 / 3 / 无记录 | `delayTasks`（含"未经系统判定"的预警任务，必须露出） |
| 激活未完成 | judge=4 | 本期不返回 |

### 2.7 后端排序（前端按返回顺序直渲即分组）

工地(project_order_id) → 品类(material_code) → 组内「考核时间−当前时间」**升序（紧迫在前）**。响应仅回 `assessmentTime`（=`platform_check_time`），不放差值。

### 2.8 并发保护（一键通知双层 CAS）

node 表无乐观锁，靠 `process_status` 条件更新（2→3）：`WHERE id=? AND process_status=2`，affected=1 才成功；失败即"已被其他路径处理"，前端需明确提示。task_dispatch 同理二次校验。

---

## 3. 接口清单

| # | 接口 | 状态 |
| --- | --- | --- |
| 1 | `POST /api/deduct/task/result` — 接收识别结果落库（一期） | ✅ 已开发提交 |
| 2 | `POST /api/material/notify/task/batch/query` — 批量查询通知任务详情（二期） | ✅ 已开发提交 |
| 3 | `POST /api/material/notify/task/summary` — 可通知/延期计数摘要 | ⏳ 未开发 |
| 4 | `POST /api/material/notify/task/batch/submit` — 一键通知批量执行 | ⏳ 未开发 |
| 5 | `POST /api/material/notify/task/batch/submit` check（检测是否已从其他路径执行） | ⏳ 未开发 |
| 6 | auto-fill / completed/query / snapshot/query | ⏳ 未开发 |
| 7 | 09:10 轮巡 push 缓存 | ⏳ 未开发 |

---

## 4. 开发改动记录（代码）

> 两笔提交均在工作分支，**未推送**。提交清单见 `git log --oneline -5`。

### 4.1 一期：上游通知判定结果消息落库（commit `b4ac1170a`）

```
[紧急]上游通知判定结果消息落库。  12 files changed, 1754 insertions(+)
```

| 模块 | 文件 | 内容 |
| --- | --- | --- |
| api | `NotifyTaskAutomationFeign.java` | `@FeignClient("edar-starlord")`，`/api/deduct/task/result` |
| api | `NotifyTaskResultParam.java` | 请求体（下划线命名，与落库列一致） |
| base | `NotifyJudgeResultEnum.java` | 4 值枚举 |
| dao | `MaterialNotifyTaskAutomation` Model/Example | 按 DDL 建模；`key_frame_urls` String 逗号分隔；DATE 用 `java.util.Date` |
| dao | `MaterialNotifyTaskAutomationMapper.java` + XML | `extends BaseMapper`，`insertSelective` |
| dao | `MaterialNotifyTaskAutomationDao(.Impl)` | `CallRecordDaoImpl` 模式 |
| service | `NotifyTaskAutomationService(.Impl)` | 字段映射 + 落库（keyFrameUrls 逗号拼接；recommend_visit_time=T+3） |
| web | `NotifyTaskAutomationController.java` | `implements` Feign |

### 4.2 二期：批量查询通知任务详情（commit `7b3c46d69`）

```
[紧急]根据工人，查询工人向全部工地的可通知任务和延期任务列表。  16 files changed, 500 insertions(+), 24 deletions(-)
```

| 模块 | 文件 | 内容 |
| --- | --- | --- |
| api | `NotifyTaskQueryFeign.java` | `/api/material/notify/task/batch/query` |
| api | `NotifyTaskQueryParam.java` | 仅 `task_type`（原值拼接串）；`executorId` 走登录上下文 |
| api | `NotifyTaskBatchQueryVO.java` | `notifyTasks` + `delayTasks` |
| api | `NotifyTaskDetailVO.java` | 详情字段 |
| api | `NotifyTaskResultParam.java`（改） | `taskType` Integer → **String** |
| base | `NotifyJudgeResultEnum.java`（改） | 判据语义对齐 |
| dao | `MaterialNotifyTaskAutomationQueryCondition.java`（新） | 内部静态类 `Dimension(projectOrderId, materialCode)` + taskType + receiveTime |
| dao | `MaterialNotifyTaskAutomationDao(.Impl)`（改） | `selectLatestBatch` → `selectTodayByProjectAndMaterial` |
| dao | `MaterialNotifyTaskAutomationMapper.java` + XML（改） | 新增精确维度 select + 行构造 IN |
| dao | Model/Example（改） | task_type 改 String |
| service | `NotifyTaskBatchQueryService(.Impl)` | 查询主体 node 表 + automation 补判定 + 归组 + 嵌套排序 |
| web | `NotifyTaskBatchQueryController.java` | `implements` Feign |

### 4.3 关键设计与改造决策

1. **task_type 存量改 String**：推翻 HEAD 一期 Integer 口径，接收/落库/查询/响应全链路改字符串原值；DDL `VARCHAR(16)`（见 `执行方案.md §2` ALTER），索引跟随无需重建。
2. **automation 关联改精确维度**：由「项目 IN × 品类 IN 笛卡尔范围 + 内存取最新」改为「精确 `(project, material, task_type)` 维度 + `receive_time=当天`」，避免范围查询误扫到非目标项目/品类记录。
3. **receive_time 取当天零点**：`DateUtil.parse(DateUtil.format(new Date()), DEFAULT_DAY_FORMAT)`，与一期落库口径（`yyyy-MM-dd` 解析）一致；传 `new Date()` 会导致 MySQL DATE/DATETIME 比较永不命中。
4. **查询主体是 node 表而非 automation**：延期预警含"未经过系统判定"的任务，automation 无记录也必须露出；automation 只做判定字段补充。

### 4.4 未提交（注意！）

- `edar-starlord-service/.../service/NotifyTaskTypeUtil.java`（`parse`/`pack` 单点工具）仍为 `??` 未跟踪，**提交 `7b3c46d69` 的 ServiceImpl:64 依赖它**，检出该提交会编译失败。**待补提交。**

---

## 5. 遗留待办

- [ ] 补提交 `NotifyTaskTypeUtil.java`（当前工作区缺失于 HEAD）
- [ ] DBA 建表：`material_notify_task_automation`（task_type 用 VARCHAR(16)，含 `idx_receive_time`、`idx_project_task_material`）；node 表 ALTER 加 `ai_adoption_type` / `notify_execute_type`
- [ ] 一期接收接口 `/api/deduct/task/result` 是否从登录校验排除（Feign 模拟器联调）
- [ ] 二期 batch/submit 一键通知（含双层 CAS + check），summary / auto-fill / completed / snapshot 接口
- [ ] 09:10 / 13:00 / 19:00 轮巡 push（复用 `PushClient#sendPushMsg`）
- [ ] 开城开关上线：`notification.remeasure.automation.open-city-engineering-forcemanucid-dept`
- [ ] 需求文档内埋点设计（`需求背景.md` 后半）确认是否按期落地