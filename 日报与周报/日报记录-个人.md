发送给需求层的消息是同一订单下，SKU-供应商-店铺列表

---

在新老商复制的逻辑当中，同一项目、同一品类下，新商任务是从创建取消列表处理逻辑中加进去的。但此时加进去并没有被测量的数据。所以在复制的时候，需要将老商的任务「测量」复制给新商。
20260720
由定时任务执行T+1任务-ProcessT1Batch。然后通过query yesterday ending去查询那个中间表，来得到昨天的变化增量pending。之后进行process处理。首先通过Aggregate的聚合，聚合得到初始状态和最终状态的一个变化，比如A到B、B到C、C到D，聚合为A到D、B到D、C到D。将聚合结果记为Aggregate.之后将aggregate中的数据按skuId_merchantId分组。下一步就查询这些被增量影响的订单。通过queryOrderRange调用hive API去查询hive数据。根据查到的结果进行处理。若是没有查到，则将中间表supplierReplaceMessage中这些增量数据mark为已完成。若查到数据，则针对这些数据按照Project order ID进行分组。以Project order ID为维度进行分批处理。然后得到同一订单下，以SKU和Merchant ID分组的被影响的订单。被影响的订单拿到之后，再根据刚才聚合的结果拿到替换数据。然后对其进行履约结果的判断。分为新商履约和老商履约。若为老商，直接放入完成列表doneByKey中。作为新商则放到待处理列表fulfillByKey中。之后就是针对待处理列表的处理，然后将其背联成一个Param然后调用一次需求侧。若发送成功，则在sendByKey记录成功。若发送失败，则在errByKey记录失败。之后我们就可以按照聚合Aggregate的维度进行标记。分别按照send by key、error by key和done by key三个维度去进行标记。其中send by key是发送成功的，error by key是发送失败的，done by key是老商完成的。其中有几个逻辑为，老商不做处理，默认标记为完成。

关于这个判断逻辑。通过中间表这个聚合消息拿到它的汰换类型，分为常规、紧急、紧急立即。每一种情况都会有一个方法去判断是新商履约还是老商履约。
```java
switch (replaceType) {  
    case NORMAL:  
        return judgeNormal(agg, hiveData);  
    case URGENT_IMMEDIATE:  
        return judgeUrgentImmediate(agg, hiveData);  
    case URGENT_NOT_IMMEDIATE:  
        return judgeUrgentNotImmediate(agg, hiveData);  
}
private List<MeasureApplyDetailBO> addRangeList;  
private List<MeasureApplyDetailBO> cancelRangeList;  
private List<MeasureApplyDetailBO> modifyRangeList;
```
执行完之后，有一个回调任务。是需求层毁掉Starload的。需要再次进行分析。回调参数中会给我们增加范围的数据、取消范围的数据以及修改范围的数据。
在这个process逻辑当中，首先是进行新增和取消的逻辑处理。处理完成之后，再进行SKU不改变，只改变供应商的逻辑处理。之后，货主采集考核安全管理并行处理，处理完成之后，再处oms货单。
首先梳理新建取消逻辑。「创建和取消分别嵌套在生成主材任务和下单这两个逻辑当中。」
	分为创建、取消「这个使用的Create task是根据测量申请单生成主材任务」。
		首先判断是否开城，以及是否进排程。若不进排程，则执行旧的创建逻辑-Create taskold，如果未开城，直接返回。然后以项目ID作为key，加分布式锁。然后循环遍历每一个create list。判断是否已经下过单-「根据订单+品类+供应商维度，去查数据库，判断是否有Enter Install类型的任务」。之后判断：走哪种业务模式？是Home2.5还是home2.5用工管理还是材料排程？根据不同的业务模式生成主材任务。之后需要判断是不是新的复尺流程，便进行打标。如果是新的复尺流程，需要找到复尺任务的start节点，然后判断这个start节点是不是供应商。如果是供应商的，就打标，不是供应商就不打标。之后处理取消列表，匹配 supplier + material，且状态 ∈ {未激活, 未完成, 暂停}为可取消任务。之后批量取消释放redis锁
	以及创建取消下单任务「### `createOrderTask`」
		它的核心功能就是通知作业中心创建或者取消下单相关的派工任务。判断是否开城。如果开城，则继续往下走。通知作业中心处理派工（设计/报价变更等）并通知作业中心处理派工（设计/报价变更等）
	针对只修改供应商的订单表，对其进行处理。
		该层也是技术方案第二层判断逻辑通过对不同场景下新老商任务的处理进行汇总处理-生成新商任务、取消老商任务
	问题：还未梳理清楚handleSceneAIfNeeded中新商任务与老商任务的复制逻辑，以及新商有某一个taskType，而老商没有导致的NPE
		

`排程模式下，材料按品类流程配置走： 测量 → 复尺 → 设计 → 报价 → 生产 → 发货 → 安装 .每个节点有预计时间，排程系统统一调度,材料从下单到安装，要经过很多环节（测量→设计→生产→发货→安装）。排程就是给这些环节排一个时间表：什么时候该测量、什么时候该发货、什么时候安装。`
`开城:这个城市的业务用新系统还是老系统？公司在全国各城市开展家装业务。每次系统升级，不会一次性全国上线，而是一个城市一个城市地切换。已经切换的城市叫"开城"，没切换的城市还在用老系统。`
`材料进排程：即使一个城市已经"开城"了，也不是所有订单的材料都立刻走排程。可能先让一部分订单试跑，其他的还是走老流程。"材料进排程"就是控制这个的——这个订单的材料，走不走排程？`
需要根据不同模式下来选择不同的模板，去生成任务

问题：modifyRangeList为什么没处理？针对处理主材任务的判断逻辑是否在需求层完成？处理在starlord，但是处理又分为根据测量申请单生成主材任务、根据测量申请单生成下单任务？对不对？以及针对ab场景下任务取消的逻辑？

```java
SupplierReplaceAggregateDTO agg = SupplierReplaceAggregateDTO.builder()  
        .skuId(first.getSkuId())  
        .merchantId(first.getMerchantId())  
        .oldSupplierCode(first.getOldSupplierCode())  
        .oldSupplierName(first.getOldSupplierName())  
        .newSupplierCode(last.getNewSupplierCode())  
        .newSupplierName(last.getNewSupplierName())  
        .categoryCode(last.getCategoryCode())    //为什么换品类，不是供应商汰换吗？？这个用新旧都一样
        .categoryName(last.getCategoryName())  
        .replaceType(last.getReplaceType().intValue())  
        .changedAt(parseChangedAtLong(last))  
        .messageIds(messageIds)  
        .build();  
result.add(agg);

List<SupplierReplaceAggregateDTO> aggregates = supplierReplaceAggregateService.aggregate(messages);  
if (CollectionUtils.isEmpty(aggregates)) {  //我目前理解的是防御性编程，但是为什么会没有聚合数据呢？
    log.info("[供应商汰换T+1处理] 聚合结果为空");  
    return;  
}
```

定时任务T+1:processT1Batch，在supplierReplaceMessage表中查询昨天的数据messages，
查到数据后进入process
然后聚合一下A->D放在aggregates「开始变到什么」。
aggregates根据sku-merchant分组得到aggByKey。
然后通过HiveAPI一次性查询所有受影响订单hiveDataList。
之后进行最终处理：分为三组：新商履约、老商履约直接记录doneByKey、失败。「判断为新商fulfillByKey记录，doneByKey老商」
	将查到的HiveDataList按订单号分组orderByProject。
	遍历这一个orderProject。对该订单下所有的sku-merchant分组得到rowsByKey
	然后便利一个订单下的sku-merchant分组rowsByKey，拿到每一个sku-merchant的聚合数据，进行judge判断。按照judge结果放到不同的列表中。
	然后fulfillByKey汇总成一个 param，该订单只调一次需求层，使用param发起调用。成功和失败分别使用senrByKey和errByKey记录。均已sku-merchant为key
	然后根据记录的senrByKey和errByKey，doneByKey，对supplierReplaceMessage表做mark
	judge」未看，「fulfillByKey汇总成一个 param」也没看，发送替换消息也没看sendReplaceDemand

---

2026.7.17
完成HiveApIUtil在大禹环境上的部署，并对其完成功能测试。
梳理新版主材任务激活的业务逻辑
查询主材任务配置的自营安装角色业务逻辑梳理。
重新梳理供应商汰换技术方案

供应商汰换先收集供应商汰换消息，再聚合并判断最终履约方，交由需求层修改施工需求，最后根据需求变更调整任务并同步各下游系统。当上游发生供应商汰换时，Starlord首先接收并保存所有汰换消息，通过 T+1 定时任务将同一 SKU×店铺的多次汰换聚合为最终结果，再结合 Hive 查询的订单、货单及任务数据，根据不同汰换方式（常规、紧急立即、紧急不立即）判断最终由新供应商还是老供应商继续履约。随后，将同一订单下的汰换结果汇总发送给需求层，由需求层统一修改施工需求并发布需求变更事件。Starlord收到需求层回调后，根据履约结果完成老任务取消、新任务创建、任务状态复制、任务激活等处理，同时同步主材、考核、排程等下游系统；对于“紧急立即汰换”场景，还会通知 OMS 取消原货单并重新下单，最终保证施工需求、任务、货单及各业务系统的数据保持一致。

查询主材任务配置的自营安装角色业务逻辑，根据 SKU 和店铺信息，从主材任务配置表中查出每个物料的安装角色（由什么工人安装），支持多维度逐级降级匹配。整个降级策略就是：从最精确的"指定履约分公司 + 指定供应商"逐级退让到最宽松的"不限履约分公司 + 不限供应商"，每次只放宽其中一个维度的约束。先批量查 SKU 详情和店铺信息作为后续匹配的基础数据。按照业务类型和履约分公司分组，并行查询限制分公司和不限制分公司的配置结果。对每个 SKU 按四级优先级依次尝试匹配安装角色，从最精确的分公司+供应商配置一直降级到不限分公司+不限供应商。

新版主材任务激活主要分为材料进排程反补的情况、以及这种情况之外的「材料进排程」和「其他的」三种。
材料进排程反补--直接激活。
非材料进排程反补-「查询配置」判断能否激活。 查询配置查询所有有效的和无效的，若查不到则查询已删除的，若还查不到就跳过。查到之后按照版本号降序
材料进排程不反补--根据最新模板的激活条件的判断，若差不到就返回。
	并且验证订单状态大于主材任务配置的节点状态 且 订单不是取消状态 ，可以激活
以及else--进行shouldActive拦截和尾款拦截，
进行激活操作、查第一个未激活的节点，并激活

下周计划：进入测试与联调

2026.7.16
操作大禹环境的部署，拉取分支部署在大禹上，进行测试，使用codeLink插件进行热部署和debug。

问题：昨天出现的API进行测试时发现返回success，但是data为空的问题又出现，在昨天删除一些字段后今天依旧出现，同样在odin查询后得到数据。并且换分区查询后返回超时。定位在API侧，但是原因及解决无法定位，求助数据工厂的负责人。同时尝试加入分页参数，之前未加入是因为昨日测试时未加分页也成功了，今日加入分页超时返回的问题解决了，但是13分区查询依旧无数据，通过使用moma定位问题为字段选择和分组字段不匹配，且对大数据量表做 `GROUP BY` 计算代价很高，应该去掉——如果你的需求是查明细数据（不需要聚合去重），`group` 一开始就不应该带。
**api默认保存最新的一个pt数据，删除前一天的pt数据。**
最新情况是不加where只加上pt可以，加上sku以及mermaid超时。最终原因：Hive中的数据是落在了mysql中，因为开始没有索引，需要对其提个工单加上索引。之后进行API测试并在大禹上部署就成功了，

主材任务激活处理逻辑：已激活或已完成以及不在今天内激活的均跳过，根据任务判断有processCode，走新版激活流程。没有的走旧版激活流程
新版激活：先查询流程定义，然后按流程定义中的节点顺序激活，其中又「处理多分支、条件路由等复杂场景」这部分未清楚。
旧版激活：进行processCode二次检查，和should Activate检查。并进行尾款拦截，同意之后正式激活主材任务，根据拿到的taskdispatch查询节点列表，通过get(0)的方式查询并激活第一个节点，然后进行激活后处理
应该激活逻辑：根据配置的激活依赖分为节点验收和主材任务节点依赖。节点验收依赖需要验证订单不是取消状态以及此时订单位于的流程要先于当前配置的节点依赖。
主材任务节点依赖：查询全部依赖的节点，如果依赖节点有一个任务未完成，则不激活。全部完成则返回对应任务

---

2026.7.15
明日计划com.ke.utopia.service.v2.impl.TaskDispatchBatchCreateServiceImpl#invoke 主材任务分发（TaskDispatch）的批量创建入口 ，负责根据项目订单和材料配置信息，批量生成任务分发记录及其节点。

上午梳理一下自己的hiveAPI代码，下午找宾哥review一下

进行了数据工厂中的API创建，实践了通过API从hive数据库中查询数据的方法。


思考：新人在熟悉业务代码时，我遇到不知道如何下手的问题，不知道具体的业务逻辑的所对应的代码。尝试解决：参考ai查询的方式使用业务名称对应的英文进行搜索，如创建就是create、build等关键字、删除一般是delete、cancel等，使用这些可快速定位某一业务逻辑的具体代码实现。
思考：开发中其他系统已经实现的功能，对其进行了参考与学习。本已实现的功能做最小的改动，但是用ai review中发现计算重试次数的bug，影响：功能无大的影响，但实际执行的重试次数与设置的差1次，并且影响日志的输出，可能会导致根据日志定位问题时困难的情况。
问题：在对API进行测试时发现返回success，但是data为空的问题。排查方案：首先检查数据表是否有数据，通过写sql语句在odin上查询该API所请求的表，执行语句后发现可以正确返回数据，排除数据表无数据。锁定问题出在API上，之后对API进行修改，尝试修改请求方式由POST改为GET，结果无变化。之后减少返回数据，再次请求，成功返回。初步猜测，应该是返回的数据量太大导致的。


---


2026.7.14
考核模块，它是放在了货里面去算考核时间。考核时间针对的就是测量、复尺、考核、安装等这几个任务，计算它们的考核时间。排期模块主要是做各个任务流程的排期，比如拆除、水电、瓦木等等这些任务的排期，是一个全流程排期，有具体的任务排期和工程排期。Starlord 是做一个主材任务的配置，以及主材任务的全流程。安装任务现在是放在cube施工包里面，作为 Starlord 的下游系统。

今日目标：梳理供应商汰换需求的部分代码业务逻辑

- **明细 API**：查原始单条、逐条原始业务数据（流水、订单、记录详情）原始数据存在 Hive，Hive 查询速度慢，所以做数据同步：Hive 明细数据 → 同步到 MySQL / HBase真实查询不走 Hive，直接查 MySQL/HBase
- **指标 API**：查提前聚合、统计好的汇总数值（总额、均值、转化率、销量等统计指标）
营商汰换需求中，从hive上拉出的逻辑是新建一个中间表，然后通过字段映射把数据推送到中间表当中，新建一个API去拉取这个中间表的数据。

申请了api应用的权限、梳理了从hive表中捞取数据的逻辑和流程。初次尝试从hive申请应用和api权限。


[[starlord项目整理/接口逻辑梳理/创建延期单逻辑]]

[[需求/供应商汰换20260709/从Hive拉取数据的API模式（searchHiveApi）]]




2026.7.10
今日计划：根据近日梳理的业务流程页面，尝试走一遍流程。今日拿到了供应商汰换的设计方案，明天结合业务背景理解设计方案涉及哪些表，上游是什么，下游是什么
[[需求/供应商汰换20260709/一些核心概念]]
梳理了一个整装排程设置口所对应的三个数据库，不同品类下的品类流程存储的位置。
梳理starlord项目中两套枚举类、和上下游系统
梳理材料配置页面的接口及调用的数据库
其中n_material_process_define 拿到任务类型：下单接单备货...
n_material_node_cfg：开始、通知安装、安装节点。
n_material_route：节点间路由
n_material_task_cfg：节点的任务属性：角色、操作项、重启规则、合并规则
n_material_time_cfg：时间配置如自然日工作日、之前还是之后
n_material_time_relation：时间节点：验收、考核等
n_material_push_cfg：消息推送配置 这几张表是用来记录材料履约流程、节点属性的

在小工具开发时，发现有些接口的返回数据很大，并且有些接口查询数据重复。但是前端使用本次请求的数据时可能只用一小部分，但下一个页面渲染调用的接口使用的数据可能就是第一个接口返回中没用到的数据，这种情况一旦变多，数据变大则会出现页面加载缓慢、浏览器内存增加、CPU增加等问题。但这种情况也不一定是坏事，如果第一次请求数据用了缓存，下一次请求刚好用了这些数据，这就是预取（Prefetch）但取决于第二次发生的概率，可根据埋点统计页面跳转路径来决定哪些数据适合预取。目前这个小工具使用的是整合大体量数据接口的数据，将整理逻辑放在前端，相比调用两个专职接口而言，操作流程时间缩短提升40%。

听取了部分同学的年中述职汇报，听到如何保证vibe coding的质量以及对ai改动代码的范围的控制问题。参考得到一个vibe coding链路，但是效果待实践验证，1.ai阅读需求 2.ai阅读设计文档 3.ai建立调用链路 4.ai输出影响分析 5.人确定修改范围 6.ai编码（限制目录与文件） 7.ai自我review 8.ai生成测试 9.运行静态检查 10.人工review+提交


----
---

2026.7.9
明天暂且继续梳理数据库，与业务逻辑对上。然后学习整体流程，记得记录，然后根据整体流程来梳理项目的边界职责

明天了解整个业务流程之后，自己可以尝试着梳理一下有哪些场景？可以写上，然后根据这些梳理出来的场景去做逻辑比对。

进展：1. 抓取web模块全量的请求接口，根据注释标注接口作用，输出《starlord接口字典文档》。问题：使用RestfulToolkit插件查询无法导出，一次性查不全。解决：hermes写个脚本抓取
2.学习主材任务流传流程
报价、选品、复尺、下单、供应链接货、备货、发货、安装、验收。
通知复尺、复尺、复尺完成。通过类似的服务单实现，服务单主要信息包括测量人员、场地、时间。
复尺后可能会变更、生成报价变更单包含要修改的商品信息，如尺寸、数量等。
确品之后自动下单，生成订单号。供应商侧进行接货备货发货。
发货流程需要调控场地的施工进度，发货有时进行预约发货。
安装时以施工包为单位，施工包调控工人和材料，涉及到施工包的生成、变更、延期、验收
以及施工包的拆包和合包。

整理13张流程配置页面，梳理业务逻辑关系
starlord以主材任务为起始，主材任务创建后 疑问1 有了主材任务后是否自动通知复尺？在对分公司履约配置之后按照配置后的流程走
材料首先在履约排程中设置流程，然后可以在材料调度中修改节点，涉及到施工包的情况在用工平台修改。

3.整合主材报价单申请接口获取了sku，品类等信息整合进小工具，优化报价、选品逻辑，根据拿到的主材报价单中的商品信息来判断是否经过了报价选品阶段，并且可跳转至主材报价单查询界面进行详情查询

明日计划；根据近日梳理的业务流程页面，尝试走一遍流程。今日拿到了供应商汰换的设计方案，明天结合业务背景理解设计方案涉及哪些表，上游是什么，下游是什么






----------


2026.7.8
明日计划，学习了解整体业务流程，拍不少于10张照片。
根据业务流程梳理5个常用接口，以及对应的表，关注接口的作用。

今日计划
新商配置，及用量标准。数据库底层关系，以及修改数据后对系统的影响
梳理5张表以上，生成文档，与配套的材料配置的关系
bug：小工具乱码问题。解决
功能+施工包code+报价可跳转https://fcn.ke.com/pc/website/online/scm_merchant/product_list+根据报价拿到报价选品（有无生成报价单）

施工包开发：使用之前的主材任务接口也就是https://i.starlord.home.ke.com/material-task/v2/page-task-detail，但是区别是这个施工包code查询使用这个body```
```{
"pageNum": 1,
"pageSize": 200,
"taskDispatchId": "40890295",
"nodeStates": [
"1"
],
"taskStates": [
"1"
]
}
```

taskDispatchId为主材任务接口查询的返回数据中的taskDispatchId。触发施工包查询的前提是判断主材任务接口查询的返回数据中taskName为安装。若不是则不触发查询施工包code其他不变。

报价可跳转https://fcn.ke.com/pc/website/online/scm_merchant/product_list+根据报价拿到报价选品（有无生成报价单）未完成




1.先整理数据库表为execl，打标签 《全库表分类清单.xlsx》
2.把表和表之间的关联键找出来，核心表关联关系草图
3.使用RestfulToolkit插件查询所有的http接口，接口返回字段 整理接口清单.xlxs 以及业务所需字段清单
4.正向追踪链路接口，明确接口、接口返回字段、业务含义、来源表、数据库字段、处理逻辑 输出接口数据库字段映射表
5.反向搜索数据库表 输出接口数据库字段映射表



搞清楚这个材料的各个节点数据来自哪里，每个节点的流程来自哪里，流程配置的数据来自哪里

| 接口                                        | 查询               | 查哪张表                                                                                                                                                                                                                                               |
| ----------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| /material-task/config/template-list       | 分页查询模板详情<br>查到当前 | n_material_template：拿到模板id<br>n_material_template_unit:用工任务模板维度表(存储的数据和作用未知)<br>n_material_process_define：拿到任务类型：下单接单备货...                                                                                                                         |
| /material-task/config/process-define-list | 查询主材任务流程         | n_material_process_define <br>n_material_node_cfg：开始、通知安装、安装节点。<br>n_material_route：节点间路由<br>n_material_task_cfg：节点的任务属性：角色、操作项、重启规则、合并规则<br>n_material_time_cfg：时间配置如自然日工作日、之前还是之后<br>n_material_time_relation：时间节点：验收、考核等<br>n_material_push_cfg |
| /material-task/config/select-option       | 枚举接口             |                                                                                                                                                                                                                                                    |
| /material-task/config/examine-one-option  | 考核时间选项一级列表       |                                                                                                                                                                                                                                                    |
| /material-task/config/examine-two-option  | 考核时间选项二级列表       |                                                                                                                                                                                                                                                    |
| /material-task/v2/page-task-detail        | 获取任务详情           | `task_dispatch`主材任务数据表：任务类型、节点类型、材料名<br>`task_dispatch_node`主材任务节点：节点类型、                                                                                                                                                                           |
| /api/construction/task/create             | 创建主材任务           | 调用cube生成施工包，逻辑为一次创建需要一些配置信息，通过一些查询其他的表得到，哪些表未知，明日搞清楚。<br>task_dispatch                                                                                                                                                                             |
|                                           |                  |                                                                                                                                                                                                                                                    |



新商配置及用量判断逻辑
一、系统逻辑调整依据
用量判断标准：用量判断决定任务生成流程，若商品数量为0（如木门）则无需触发后续履约流程。
判断来源：用量数据需从系统特定模块提取，中控，需明确当前判断逻辑的具体实现路径。
新旧商切换规则：老商切换新商后，需根据新商配置判断是否继承原任务（如复尺任务），未配置则不生成任务。
二、配置验证流程
新商配置查询：切换新商后需查询当前分公司对应供应商的新商配置信息。
配置匹配条件：验证新商是否具备承接原任务的能力（如复尺能力），若未配置则终止任务生成流程。
配置依赖关系：任务生成必须基于线上配置状态，新商未配置功能则对应任务不可执行。


以昨天的小工具为抓手，分析/material-task/v2/page-task-detail调用链路及数据库，该接口调用了`task_dispatch`主材任务数据表以及`task_dispatch_node`主材任务节点。其中复尺等流程节点来自task_dispatch中的task_type。
为什么返回的数据中会有taskDispatch中type_name 和taskNodeDetailList中node_name一样？原因：NodeTypeEnum.getAliasName会判断节点类型，部分状态会替换
```
NODE_START(1, "开始节点","开始"), // 开始  
NOTIFY_CAN_START(20, "通知启动","约工"), // 约工  
NOTIFY_START(40, "启动派单","派单"), // 派单  
SELF_CHECK(50, "进场","进场"), // 进场  
START(60, "启动","提交自检"), // 完工自检  
SELF_CHECK_ACCEPTANCE(65, "自检验收","自检验收"), // 自检验收  
FINISH_START(80, "启动验收","实地验收"), // 实地验收  
OWNER_CONFIRM(85, "业主确认","业主确认"); // 业主确认

```





-------------------------------


2026.7.7
1.根据主材履约进度，关注接单后订单信息，扩展查询工具的功能，加入订单信息查询接口，可区分一个材料履约流程中多个下单信息，优化布局至一眼看全，提高查询效率。整合 2 个接口，输入项目 ID 一键查询材料列表、类目详情、履约时间轴，从 3 页面手动查找变为 1 键查询，支持同时观测多个接单的供应商履约进度，材料履约状态查询效率提升 10 倍。查询时间由～5min提升至30s
2.参与供应商汰换的技术评审、弄明白业务背景
 +上游系统：在常规供应商汰换流程之外，业务中存在一类需要快速终止老商履约、切换新商的紧急场景（例如供应商服务异常、质量问题、突发合规风险、重大履约事故等）。这类场景无法等待老商在途货单自然消化，需要对不同状态的切换供应商进行做差异化处理，供应链需支持决策是让老商继续履约，还是立即切换给新商。
 +供应商切换时向OMS 发送通知消息。1. 消息内容：SKUID、店铺 ID、原供应商 ID、新供应商 ID、供应商汰换类型（常规汰换 / 紧急汰换）、是否立即切换（是 / 否 / 空）以及变更时间戳。
 +本项目背景：上游发起供应商汰换后，根据上游传入的汰换方式、原供应商、目标新供应商、品类信息，以及当前“品类-供应商”维度下货前任务状态，进行任务处理。![[日报与周报/fig/Pasted image 20260707120947.png]]如图所示根据不同的汰换方式以及货前服务状态进行不同的任务处理（新/老商履约）新商切换主要涉及**主材任务**生成以及**排程考核**
整体流程分为两层判断，第一层判断是新商还是老商履约。第二层判断是根据是谁履约并且结合角色场景来判断来对新老商任务的处理。其中角色场景是一个关键概念，就是指货前服务的作业角色（如项目经理、设计师、项目助理，这类属于非供应商角色）。场景分为a,b。若新老商角色一致且均为非供应商则为a，其余则为b。不同角色场景下，新老商根据老商的任务状态去进行处理，梳理逻辑如下。


 ![[日报与周报/fig/Pasted image 20260707193330.png]]

**20260706**

梳理材料配置流程，

对比starlord代码梳理10个常用的核心数据库

  

**20260703**

**学习家装业务系统相关知识，明确** 家装业务核心模型为“分类-分项-商品/工艺”三级，最终组合为“定额”呈现给用户；材料履约主流程清晰，但受工地进度、成本与人员管理影响，衍生出批量操作等复杂性；当前系统问题根源在于多变的业务管理诉求，配置复杂易错，提出分步引入AI作为根本解决方案

**核心流程为报价->选品->复尺/变更->确品/下单->接备发(约送)->装(施工包)->验收**

  

安装了昨日分享的obsidian和飞书的插件，实现了知识库与飞书文档的传递

  

**配置git插件，实现自动上传和更新  
学习了线上问题排查方法，总结排查手册**

  

**20260702**

  

今日计划：

深入了解所在业务组的具体业务场景，参与昨日评审的需求，完成业务理解的回旋镖

整理starlord新人学习文档，帮助理解starlord系统的业务职责。

理清楚中控和内控的区别，方便理解系统业务流程，

整理Home全局学习新人文档，帮助理解Home中心的业务逻辑。

梳理施工包的概念，借用其内部包含的东西以及与其他包的对比进行理解。

疑问：交付中为什么全是主材的履约流程，辅材呢？辅材不履约。

  

|   |   |   |
|---|---|---|
|维度|中控|内控|
|全称|中央控制系统 / 报价中控系统|内部控制系统 / 内控调量系统|
|定位|业务中台 — 报价、定额、数据汇总|内部管控 — 工程量审核、成本把控|
|核心职责|算报价、匹配定额、合并数据|审工程量、控成本、防风险|
|系统性质|业务系统（核心生产系统）|风控/审核系统（管理支撑系统）|

  

  
  
输出edar-starlord系统新人学习文档「系统职责梳理」—该系统是**家装交付中台**，核心定位是：**主材任务全生命周期管理的调度引擎和配置中心**。

参与AI围炉，学习使用obsidian+ai的模式构建自己的知识库

  

  

—————————————————————————————————

  

**20260701**

**今日需完成的任务**

亲爱的刘腾达，欢迎您正式加入贝壳，成为与我们并肩作战的优秀贝壳人。现需要您在15个自然日内与直接上级完成试用期180天内的目标设定沟通哦。并登陆电脑PC端：jixiao.ke.com完成线上填写哦~期待您更优秀的表现哦~

|   |
|---|
|尝试 Obsidian 以及 Hermes-Agent在edar-starlord 项目中应用|

获取maven的私有库

初步了解edar-starlord项目的意义，计划一周内摸清，明白自己未来的方向

  

  

**今日计划：**

学习了解hermes agent，安装完成，与飞书配置完成；

安装obsidian，使用obsidian输出edar-starlord项目的文档

创建可复用的code-to-wiki skill。

初次上线，发布edar-starlord，构建-预发-预发验证-灰度发布-灰度验证-生产发布。通过fast日志平台在验证时观察有无报错，15分钟无问题可进行下一步。灰度下若报错，需要区分报错是否由本次发布引起的，可对比其他服务器有无相同报错。

edar-starlord 交付中台，管理从量房、物料任务创建、调度派工、安装施工、验收到延期的完整交付生命周期

  

交付系统所执行的任务是自签单完成后执行内控调量、生成施工包、材料采购计划下发之后由供应链模块进行材料配送，之后交付系统中的用工平台与人员系统对接执行工人调度、约工、进场、施工、验收、结算以及售后。

  

初次参加供应商汰换的交互细节以及对上游的依赖需求评审，了解了需求的业务背景以及需求从出现到排期、方案、开发、联调等步骤。

  

完善新人入职指引中keones上线发布权限配置的指引

  

  

  

**问题；**

不清楚harness 以及hermes的区别：harness Engineering 是对LLM的控制、约束，可理解为是一种思想，核心是把过去零散的、口头的规范，变成 AI 能读取和遵循的系统化规则。

Hermes Agnet 会自我进化的 AI 助手，可类比openclaw--**Token 消耗大、账单吃不消**，以及**长时间工作稳定性差、经常失联**。原因是它的设计哲学——**连接广度优先**。而hermes agent它的设计思路完全不同，**学习深度优先**。它不只记录“发生了什么”，更记录“怎么做的才有效”。通过内置的 **“闭环学习循环”** ，Agent 能从每次成功执行的任务中自动提炼出可复用的技能，越用越强。

  

**思考：**

**如何快速掌握当前的业务？**

分四步：

1.首先了解整个最大项目所完成的事情

2.而完成这个事情又把这个大项目拆成了几部分（完成这个事情需要几步）以及先后顺序

3.你所在的部分要与前后节点的通信，需要什么数据，有几条通信接口

4.所在的系统需要保存的数据

  

**定位问题以及解决1**

三个明确：

明确具体问题，不要管系统的问题

明确什么原因引起

明确产生什么影响

  

可对问题快速掌握

  

明日计划：

深入了解所在业务组的具体业务场景，参与今日评审的需求，完成业务理解的回旋镖

  

  

———————————————————————————————

  

  

**20260630**

今日计划：

阅读新人培养计划

  

安装Claude coede

工作环境搭建（git、gitlab、maven、IDE（javaIDE、vs code等）、navicat、VPN、Charles/fiddler、postman等，以及部分办公工具）

5、加入群\邮件组，配置Claude code，初步使用deepseek模型，遇到问题：deepseek有(OpenAI 格式)**(**Anthropic 格式)，claude仅支持Anthropic 格式。更换baseurl后配置成功。

了解了obsidian以及Hermes，计划下一步尝试应用。初步了解家装业务，配置gitlab账户，拉取edar-starlord项目，计划明天进行熟悉项目，以及进一步了解家装、所在业务组的具体业务

  

明日计划

|   |
|---|
|尝试 Obsidian 以及 Hermes-Agent在edar-starlord 项目中应用|

获取maven的私有库

初步了解edar-starlord项目的意义，计划一周内摸清，明白自己未来的方向

————————————————————————————