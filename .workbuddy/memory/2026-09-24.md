# 2026-09-24 工作日志

## teach 技能：jenkins 教学空间
- 用 `/teach jenkins` 在 `/Users/mirror/wiki/jenkins-teach/` 新建教学空间（复用 kafka-teach 的 `assets/style.css`，改深蓝色值）。
- 已落文件：`MISSION.md` / `NOTES.md` / `RESOURCES.md` / `learning-records/0001-baseline-beginner.md` / `reference/jenkins-glossary.html` / `lessons/0001-build-to-pipeline.html`（已 open 打开）。
- 用户画像（lesson 1 确认）：完全没用过 Jenkins，目标「从零搭一条 Java(Maven) 流水线」。
- 真实素材锚点：`edar-starlord` 工程——多模块 Maven、Spring Boot 2.1.9、Java 8；打包靠 `build.sh`(`mvn clean package -Dmaven.test.skip=true`→`release.tar.gz`)、大禹平台 `build-dayu.sh`、`Dockerfile` 基于 centos_java_1_8。
- 关键教学点（已写进 lesson）：Jenkins 主程序需 Java 17/21，与构建目标 Java 8 是两套 JVM；声明式 `agent` 必填。
- 下一步（待用户）：第二课本地跑起 Jenkins + 新建 Pipeline 跑通 Maven 构建。

## lesson 2 已产出
- `lessons/0002-install-and-first-build.html`（已 open）。主题：brew 安装 jenkins-lts（依赖 openjdk@21）+ 解锁 + hello world + 接本地 git(`file:///Users/mirror/IdeaProjects/edar-starlord`) 跑通 Maven 构建。
- 已核实本机事实：`brew info jenkins-lts` → 2.568.3，依赖 openjdk@21；Jenkins 2.568 需 Java 21/25（Java17 已弃用，来源 jenkins.io changelog/support-policy）。
- 教学决策：Jenkinsfile 用 `./mvnw`（wrapper）而非 `mvn`，规避本机未装 Maven / PATH 问题；明确"Jenkins 跑 JDK21、项目编译目标 Java8 是两套 JVM"的易混点。
- 待确认：是否要我直接帮他跑 `brew install jenkins-lts` 并启动（环境变更动作，已留口子让用户决定）。

## lesson 3 已产出（用户问"jenkes使用教程"）
- `lessons/0003-daily-usage.html`（已 open）。主题：Jenkins 日常使用教程——任务类型对比(Freestyle vs Pipeline)、Pipeline 任务配置项(SCM/分支/Script Path)、构建触发器(手动/SCM轮询/Webhook 对比表)、参数化构建(parameters)、读结果(状态球/Console/Changes)、制品归档(archiveArtifacts)、workspace/构建历史清理、高频排障清单。
- 关键提醒（已写课里）：本机 127.0.0.1:8080 的 Jenkins 无法被公司 GitLab webhook 触发，需 SCM 轮询或部署到互通机器。
- 已同步更新 NOTES.md 课程进度段。
