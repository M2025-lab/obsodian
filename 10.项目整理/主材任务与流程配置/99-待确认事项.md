# 99 待确认事项（代码无法判定，需线下确认）

> 以下事项在 edar-starlord 仓库代码中无法给出确定结论，已从正文结论中分离。确认后请回填对应文档。

| # | 事项 | 背景 | 建议确认方式 |
|---|---|---|---|
| 1 | 老画图入口是否已废弃 | `manpower/cfg/processDefineSave`、`category/process/route/save` 代码仍在但属老体系 | 问前端/产品确认线上入口 |
| 2 | OFC 供应链侧模板如何维护 | `OfcDispatchConfigMergeApi` 在外部依赖 jar，模板定义在 OFC 服务内 | 找 OFC 侧同事 |
| 3 | `n_material_process_template` 本地表的写入机制 | 本仓库零写入，判定为 OFC 镜像；三种可能：共享库直写 / DBA 脚本 / 数据管道 | 找 OFC 侧或 DBA；可用线上 template_id 与 OFC queryTemplateList 返回的 templateId 对比验证 |
| 4 | `material_flow_rule_template_mapping` 的生产入口 | 写入分散在 processSave / copyCategoryTemplate / categoryInfoUpdate，web 层只有 TestController 注入 | 问后端同事或查调用方服务 |
| 5 | `/api/construction/task/create` 逐字段入参说明 | 本次调查只核对了链路，未逐字段展开 | 需要时另行逐字段核对 |
| 6 | deliveryflow 与 materialflow 两套模板入口生产上用哪个 | 两套并存，代码层面无法判定 | 问前端/产品 |
