# 📚 数据库迁移方案：八合一零售配置 → 排程配置（代码迁移版）

> **迁移对象**：八合一 `material_flow_rule*` 零售配置 → 排程 `delivery_flow_rule*` 配置
> **一句话结论**：采用代码迁移，以单个 `rule_id` 为独立事务单元，逐条迁移、插入失败即回滚该 rule，保证可重试、可回滚、可校验。
> **沉淀日期**：2026-09-03
> **关联笔记**：[[📚 ....md]]

---

## 0. 方案概览与待定项

### 0.1 方案定位

本文档为**代码迁移版**方案。核心思路：

- 以 `rule_id` 为最小迁移单元，**一个 rule_id 一个事务**（`@Transactional` 包裹）。
- 事务内依次插入 `delivery_flow_rule` → `delivery_flow_rule_unit` → `delivery_flow_rule_category`，任一插入失败，整个 rule 的写入回滚。
- 迁移全程只对排程表做**新增写入**，不修改源八合一表（只读）。
- 单条失败不影响其他 rule，失败记录落日志表，可断点重试。

### 0.2 关键待定项（影响迁移逻辑，需业务确认）

| 编号  | 待定项                                   | 候选方案                                                              | 影响面                                               |
| --- | ------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------- |
| Q1  | **迁移类型边界**                            | A：纯零售（`sale_type=12`）<br>B：零售类（`sale_type=12` 全量，含4\12\22\29\30等） | 决定 `material_flow_rule_unit` 的过滤 SQL，是本方案最关键的前置决策 |
| Q2  | `delivery_process_cfg_id`（排程流程配置id）来源 | A：新建一张排程配置并拿其 id<br>B：复用某条已有排程配置 id                               | rule 主表必填字段，无默认逻辑                                 |
| Q3  | 失效/编辑态数据是否迁移                          | A：只迁 `state=1`（有效）<br>B：全量迁移并做状态重映射                               | 影响迁移量与状态映射逻辑                                      |

> ⚠️ Q1 是方案能否开工的前提，下文默认按 **A：纯零售** 描述，业务确认后可调整。


## 1. 迁移范围与前置条件

### 1.1 迁移范围（表对表）

| 源表（八合一，只读）                            | 目标表（排程，写入）                    | 关系                                               |
| ------------------------------------- | ----------------------------- | ------------------------------------------------ |
| `material_flow_rule`                  | `delivery_flow_rule`          | 主表 1:1                                           |
| `material_flow_rule_unit`             | `delivery_flow_rule_unit`     | 元素表 1:N（按 rule_id）                               |
| `material_flow_rule_category`         | `delivery_flow_rule_category` | 品类表 1:N（按 rule_id）                               |
| `material_flow_rule_template_mapping` | —                             | 只读，用于查询 `n_template_code`（**不迁移**，排程无对应表）        |
| `n_material_template`                 | —                             | 只读，用 `n_template_code` 查 `template_code` 得到 `id` |


### 1.2 源数据筛选条件

> ⚠️ 字段语义注意：八合一 `material_flow_rule_unit.sale_type` 字段名虽叫 `sale_type`，但**实际存储的是 document_type 的值**（1:整装,2:零售）。因此源侧过滤零售仍用 `sale_type=2`，但其语义等同于排程侧的 `document_type=2`——这也是 §2.3 中排程 `document_type` 直接用该值回填的依据。

按用户描述"通过 Unit ID 去查询零售配置下的所有 ruleID"，再按 `sale_type=2`（零售）过滤：

```sql
-- Step1: 取零售配置下的所有 rule_id（按 Uni ID 维度）
SELECT DISTINCT rule_id
FROM material_flow_rule_unit
WHERE sale_type = 2          -- 零售（字段存的是 document_type 值；待定 Q1：纯零售 vs 零售类）
  AND state IN (1)           -- 待定 Q3：是否只迁有效
  AND rule_id IN (
      SELECT id FROM material_flow_rule WHERE state IN (1)
  );
```

### 1.3 前置条件（开工前必须就位）

1. **数据库备份**：对 `material_flow_rule*` 和 `delivery_flow_rule*` 两组表全量 dump，归档备查。
2. **排程配置就绪**：Q2 的 `delivery_process_cfg_id` 已确定（新建或复用），并记下该 id。
3. **幂等设计**：代码需支持按 `源 rule_id` 查重，避免重试时产生重复数据。
4. **灰度环境**：先在测试库跑通全量校验，再上预发/生产。
5. **日志表就绪**：建好迁移日志表，记录每条 rule 的迁移状态（见 §3.3）。


## 2. 数据链路与映射关系

### 2.1 源数据查询链路（八合一侧，只读）

```text
[源 rule_id]  (来自 material_flow_rule_unit.sale_type=2 过滤)
      │
      ├─① material_flow_rule          按 rule_id 取主表
      ├─② material_flow_rule_unit     按 rule_id 取 unit
      ├─③ material_flow_rule_category 按 rule_id 取 category
      │
      └─④ material_flow_rule_template_mapping
              按 rule_id 取 n_template_code
                    │
                    ▼
         ⑤ n_material_template
              按 n_template_code = template_code 取 id
                    │
                    ▼
              将该 id 作为目标的 category_id 写入排程 category
```

> 关键点：八合一的 `material_flow_rule_category.category_id` **不能直填**到排程——必须通过 ④→⑤ 拿到 `n_material_template.id` 再填入排程 `delivery_flow_rule_category.category_id`（因为排程侧该字段用于查询 process，需与八合一侧保持一致的模板语义）。

### 2.2 目标写入链路（排程侧，单事务）

```text
[事务开始]
  INSERT delivery_flow_rule          -- 用源 rule 字段 + 排程配置id
       ↳ 得到 new_rule_id
  INSERT delivery_flow_rule_unit     -- rule_id = new_rule_id，补 document_type*
  INSERT delivery_flow_rule_category -- rule_id = new_rule_id，category_id = n_template.id
[事务提交 / 失败回滚]
```

### 2.3 字段映射要点速查

| 目标表 | 关键差异处理 |
|---|---|
| `delivery_flow_rule` | 补 `delivery_process_cfg_id`（Q2）、`delivery_process_cfg_state=1`；丢 `is_default` |
| `delivery_flow_rule_unit` | `document_type`/`document_type_name` 用源 `sale_type`/`sale_type_name` 回填（源字段存的就是 document_type 值，语义一致）；丢 `merchant_id`/`merchant_name` |
| `delivery_flow_rule_category` | `category_id` 写 `n_material_template.id`（非直填）；补 `create_name`/`modify_name`（取主表）；`delivery_template_id=-1`（本次不写）；丢 `work_type`/`is_show` |

详细字段映射见 [[📚 ....md]] 中的「字段对比关系表」，此处不重复。


## 3. 代码迁移详细流程

### 3.1 事务边界设计

```text
单个 rule_id = 一个 @Transactional 事务
  ├─ 写 delivery_flow_rule
  ├─ 写 delivery_flow_rule_unit (N 条)
  └─ 写 delivery_flow_rule_category (N 条)
任一步骤抛异常 → 整个 rule 回滚，记录失败日志，继续下一个 rule
```

- 事务方法不能跨 rule：外层循环不包事务，由内层 `migrateOneRule(ruleId)` 方法加 `@Transactional`。
- 注意 Spring `@Transactional` 自调用失效陷阱：`migrateOneRule` 必须由独立 Bean 注入调用（不可同类内部 `this.migrateOneRule()`），否则事务不生效。
- 事务传播：`REQUIRED`；超时设置合理上限（如 30s/单 rule），防止单条慢 SQL 拖垮批次。

### 3.2 单 rule 迁移伪代码

```java
@Transactional(rollbackFor = Exception.class)
public void migrateOneRule(Long srcRuleId) {
    // ① 读主表
    MaterialFlowRule srcRule = ruleMapper.selectById(srcRuleId);
    if (srcRule == null) throw new BizException("源 rule 不存在: " + srcRuleId);

    // ② 写 delivery_flow_rule（补排程配置id，丢 is_default，状态映射）
    DeliveryFlowRule dstRule = new DeliveryFlowRule();
    BeanUtils.copyProperties(srcRule, dstRule);
    dstRule.setId(null);                                   // 新自增
    dstRule.setDeliveryProcessCfgId(cfgId);                // Q2 待定
    dstRule.setDeliveryProcessCfgState(1);
    dstRule.setState(StateMapUtil.mapRuleState(srcRule.getState())); // 状态映射 §4
    dstRuleMapper.insert(dstRule);                          // 写目标：排程 rule
    Long newRuleId = dstRule.getId();

    // ③ 读 unit 列表 → 写 delivery_flow_rule_unit
    List<MaterialFlowRuleUnit> srcUnits = unitMapper.selectByRuleId(srcRuleId);
    for (MaterialFlowRuleUnit su : srcUnits) {
        DeliveryFlowRuleUnit du = new DeliveryFlowRuleUnit();
        BeanUtils.copyProperties(su, du);
        du.setId(null);
        du.setRuleId(newRuleId);
        du.setDocumentType(su.getSaleType());             // 冗余回填
        du.setDocumentTypeName(su.getSaleTypeName());
        du.setState(StateMapUtil.mapUnitState(su.getState()));
        dstUnitMapper.insert(du);                           // 写目标：排程 unit
    }

    // ④ 读 category 列表 → 取 n_template_id → 写 delivery_flow_rule_category
    List<MaterialFlowRuleCategory> srcCats = categoryMapper.selectByRuleId(srcRuleId);
    for (MaterialFlowRuleCategory sc : srcCats) {
        // 4.1 经 mapping 拿 n_template_code
        Long nTemplateCode = mappingMapper.selectNTemplateCode(srcRuleId, sc.getRuleCategoryId());
        // 4.2 经 n_material_template 拿 id
        Long nTemplateId = (nTemplateCode == null) ? 0L
            : templateMapper.selectIdByCode(nTemplateCode.toString());

        DeliveryFlowRuleCategory dc = new DeliveryFlowRuleCategory();
        BeanUtils.copyProperties(sc, dc);
        dc.setId(null);
        dc.setRuleId(newRuleId);
        dc.setCategoryId(nTemplateId == null ? 0L : nTemplateId); // 核心映射
        dc.setDeliveryTemplateId(-1L);                    // 本次不写
        dc.setCreateName(srcRule.getCreateName());         // 主表带入
        dc.setModifyName(srcRule.getModifyName());
        dc.setState(StateMapUtil.mapCatState(sc.getState()));
        dstCategoryMapper.insert(dc);                      // 写目标：排程 category
    }
    // 事务提交（异常自动回滚）
}
```

### 3.3 迁移日志表与批次调度

#### 3.3.1 迁移日志表 DDL

日志表作为本次迁移的**正式映射表**，承载三件事：① 记录每条 rule 迁移状态；② 维护 `src_rule_id → dst_rule_id` 新旧映射（回滚、查重、对账的统一依据）；③ 幂等查重。

```sql
CREATE TABLE `migrate_rule_log` (
  `id`             bigint(20)   NOT NULL AUTO_INCREMENT,
  `batch_no`       varchar(32)  NOT NULL DEFAULT '' COMMENT '迁移批次号，如 20260903_01',
  `src_rule_id`    bigint(20)   NOT NULL COMMENT '八合一源 rule id',
  `dst_rule_id`    bigint(20)   NOT NULL DEFAULT '-1' COMMENT '排程新建 rule id，失败时为 -1',
  `src_state`      int(11)      NOT NULL DEFAULT '-1' COMMENT '源 rule 状态',
  `dst_state`      int(11)      NOT NULL DEFAULT '-1' COMMENT '映射后的目标状态',
  `unit_count`     int(11)      NOT NULL DEFAULT '0' COMMENT '本次迁入 unit 条数',
  `category_count` int(11)      NOT NULL DEFAULT '0' COMMENT '本次迁入 category 条数',
  `status`         tinyint(2)   NOT NULL DEFAULT '1' COMMENT '迁移状态(0:成功,1:失败)',
  `error_msg`      varchar(1024) NOT NULL DEFAULT '' COMMENT '失败原因',
  `create_time`    timestamp    NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '迁移时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_src_rule_id` (`src_rule_id`),   -- 幂等：同源 rule 只能有一条记录
  KEY `idx_batch_no` (`batch_no`),
  KEY `idx_dst_rule_id` (`dst_rule_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='零售配置八合一→排程迁移日志表';
```

> 关键设计：`src_rule_id` 建唯一索引——天然防重复迁移（重试时先查日志表，已有成功记录则跳过，已有失败记录则覆盖重试），无需污染业务表的 `create_name`。
> `status` 用数字编码：`0`=成功、`1`=失败（默认 1，便于发现漏写）。
> `dst_rule_id` 建索引——回滚时按它反查删除目标数据（见 §6.2）。

#### 3.3.2 批次调度伪代码

```java
public void runBatch(String batchNo) {
    List<Long> ruleIds = unitMapper.selectRetailRuleIds(); // §1.2 过滤
    for (Long ruleId : ruleIds) {
        try {
            // 幂等：已成功迁移则跳过
            MigrateRuleLog exist = logMapper.selectBySrcRuleId(ruleId);
            if (exist != null && exist.getStatus() == 0) {   // 0=成功
                continue;
            }
            Long newRuleId = self.migrateOneRule(ruleId);   // 注入代理调用，事务生效
            logMapper.upsert(batchNo, ruleId, newRuleId, 0, null, ...);   // 0=成功
        } catch (Exception e) {
            // 失败也落日志，dst_rule_id=0，事务内已自动回滚
            logMapper.upsert(batchNo, ruleId, 0L, 1, e.getMessage(), ...);   // 1=失败
            log.error("迁移失败 rule_id={}, 已回滚", ruleId, e);
            // 不抛出，继续下一个 rule
        }
    }
}
```

#### 3.3.3 `migrateOneRule` 返回新 rule id

§3.2 伪代码中 `migrateOneRule` 改为返回 `Long`（新建的 `dst_rule_id`），供日志表记录映射：

```java
@Transactional(rollbackFor = Exception.class)
public Long migrateOneRule(Long srcRuleId) {
    // ... 同 §3.2 ...
    return dstRule.getId();
}
```


## 4. 状态映射表

### 4.1 枚举对照

| 枚举值 | 八合一 `state` | 排程 `state` | 说明 |
|---|---|---|---|
| 0 | 编辑 | 草稿 | 语义不同但可对应 |
| 1 | 有效 | 有效 | **直接映射 1→1** |
| 2 | 失效 | 删除 / 失效 | 排程有 2=删除、3=失效两个含义 |
| 3 | — | 失效 | 八合一无此档 |
| 4/5 | — | 审核中/草稿 | 排程独有，迁移不涉及 |

### 4.2 迁移映射规则（建议）

| 八合一 state | → 排程 state | 理由 |
|---|---|---|
| `1`（有效） | `1`（有效） | 语义一致，直接映射 |
| `0`（编辑/草稿） | `0`（草稿） | 排程也有草稿态，按业务保留为草稿 |
| `2`（失效） | `3`（失效） | 选 3 而非 2，避免被当作"已删除"误清理；保留失效语义便于追溯 |

> ⚠️ 待定项 Q3：若业务决定只迁有效数据（`state=1`），则状态字段恒映射为 1，无需处理 0/2。建议优先采用此方案，降低复杂度。

### 4.3 状态映射工具类（对应 §3.2 的 `StateMapUtil`）

```java
public class StateMapUtil {
    public static int mapRuleState(int src)   { return map(src); }
    public static int mapUnitState(int src)   { return map(src); }
    public static int mapCatState(int src)    { return map(src); }

    private static int map(int src) {
        switch (src) {
            case 1: return 1;   // 有效→有效
            case 0: return 0;   // 编辑→草稿
            case 2: return 3;   // 失效→失效（避免落到"删除"2）
            default: return 0;  // 异常值兜底为草稿，记日志
        }
    }
}
```


## 5. 风险评估

| 编号  | 风险                                                      | 等级   | 影响                   | 应对措施                                                                                     |
| --- | ------------------------------------------------------- | ---- | -------------------- | ---------------------------------------------------------------------------------------- |
| R1  | **`n_template_code` 查不到 / `n_material_template` 无对应记录** | 🔴 高 | 排程侧无法关联 process      | 跳过该 rule，记 `status=1` 迁移失败，`error_msg` 写明原因（`n_template_code` 缺失或模板不存在）；上线前先跑统计 SQL 看缺失率 |
| R2  | **同一源 rule 重试导致重复写入**                                   | 🔴 高 | 数据重复，排程查询出多条规则       | 迁移前按 `src_rule_id` 查重，已迁则跳过；日志表建唯一索引                                                     |
| R3  | **`@Transactional` 自调用失效**                              | 🟡 中 | 事务不生效，部分写入无法回滚       | `migrateOneRule` 由独立 Bean 注入调用，AOP 生效；单元测试验证回滚                                           |
| R4  | **迁移类型边界 Q1 未定**                                        | 🟡 中 | 迁多了搭售数据或漏迁零售数据       | 业务确认后再执行；先在测试库跑两版过滤 SQL 对比数量                                                             |
| R5  | **`delivery_process_cfg_id` Q2 未定**                     | 🟡 中 | rule 主表必填字段为空导致写入失败  | 先确定排程配置id，否则不开工                                                                          |
| R6  | **大事务 / 慢 SQL**                                         | 🟡 中 | 单 rule 数据量大或锁等待，拖垮批次 | 单事务设超时；分批提交；避开业务高峰；监控慢查询                                                                 |
| R7  | **源数据脏值**（NULL、超长、枚举异常）                                 | 🟡 中 | 写入报错回滚               | `material_name` NULL→''；异常 state 走兜底；先跑数据质量校验                                            |
| R8  | **排程表已有业务数据，迁移冲突**                                      | 🟢 低 | 主键或业务唯一键冲突           | 目标表用新自增 id；迁移映射落 `migrate_rule_log`，按 `dst_rule_id` 反查回滚                                 |
| R9  | **并发 / 重复执行**                                           | 🟢 低 | 同一批次被重复触发            | 加分布式锁或迁移批次号；日志表唯一索引兜底                                                                    |
| R10 | **迁移期间排程侧有读流量**                                         | 🟢 低 | 读到未完成数据              | 进行开关控制，迁移之后再开                                                                            |


## 6. 回滚方案

代码迁移只对排程表做新增，回滚策略为**按批次删除本次写入数据**，分三层。

### 6.1 单 rule 级回滚（自动）

- 机制：`@Transactional`，单 rule 任意步骤失败自动回滚该 rule。
- 效果：失败 rule 不会在排程表留下半成品；日志表记 `status=1`，可单独重试。

### 6.2 批次级回滚（手动，按日志表映射反查删除）

日志表 `migrate_rule_log` 维护了 `src_rule_id → dst_rule_id` 映射，回滚时**按日志表的 `dst_rule_id` 反查删除**，不污染业务表字段。

```sql
-- 1. 取本批次所有成功迁入的目标 rule id
SELECT dst_rule_id FROM migrate_rule_log
WHERE batch_no = '20260903_01' AND status = 0;   -- 0=成功

-- 2. 按 dst_rule_id 反向删除（顺序：category → unit → rule）
DELETE FROM delivery_flow_rule_category WHERE rule_id IN (
    SELECT dst_rule_id FROM migrate_rule_log
    WHERE batch_no = '20260903_01' AND status = 0     -- 0=成功
);
DELETE FROM delivery_flow_rule_unit WHERE rule_id IN (
    SELECT dst_rule_id FROM migrate_rule_log
    WHERE batch_no = '20260903_01' AND status = 0     -- 0=成功
);
DELETE FROM delivery_flow_rule WHERE id IN (
    SELECT dst_rule_id FROM migrate_rule_log
    WHERE batch_no = '20260903_01' AND status = 0     -- 0=成功
);

-- 3. 清理日志表对应批次记录（可选，或保留作为审计）
-- DELETE FROM migrate_rule_log WHERE batch_no = '20260903_01';
```

> 优势：业务表无任何迁移标记污染；回滚范围精确到成功记录；失败记录 `dst_rule_id=0` 天然不会被误删。

### 6.3 全量回滚（兜底）

- 执行前已对排程三表做 dump（§1.3 前置条件）。
- 若整批迁移出现严重问题，用 dump 恢复或执行批次删除 SQL 清空本批数据后重跑。
- 源八合一表全程只读，无需回滚源数据。

### 6.4 回滚决策表

| 场景 | 回滚级别 | 操作 |
|---|---|---|
| 单条 rule 失败 | 单 rule | 自动回滚，日志记 `status=1`，可重试 |
| 批次内少量失败 | 单 rule | 成功保留，失败重试，不回滚整批 |
| 批次性逻辑错误 | 批次 | 按日志表 `dst_rule_id` 反查删除本批，修代码后重跑 |
| 严重数据污染 | 全量 | dump 恢复排程表 |


## 7. 验证方案

### 7.1 迁移前校验（数据质量）

```sql
-- 1. 源零售 rule 总数
SELECT COUNT(DISTINCT rule_id) FROM material_flow_rule_unit
WHERE sale_type = 2 AND state = 1;

-- 2. 源 rule 下 mapping 的 n_template_code 缺失率（R1 风险点）
SELECT COUNT(*) AS total,
       SUM(CASE WHEN m.n_template_code IS NULL OR m.n_template_code = 0 THEN 1 ELSE 0 END) AS missing
FROM material_flow_rule_template_mapping m
WHERE m.rule_id IN (上面查到的 rule_id 集合);

-- 3. n_template_code 在 n_material_template 中能否查到 id
SELECT m.n_template_code, t.id
FROM material_flow_rule_template_mapping m
LEFT JOIN n_material_template t ON t.template_code = m.n_template_code
WHERE m.n_template_code IS NOT NULL AND t.id IS NULL;  -- 有结果=映射断裂
```

### 7.2 迁移后校验（数量与一致性）

| 校验项 | 校验 SQL 思路 | 预期 |
|---|---|---|
| rule 数量一致 | 源 `material_flow_rule` 计数 = 目标 `delivery_flow_rule` 本批次计数 | 相等 |
| unit 数量一致 | 按 rule_id 分组，源/目标 unit 条数对齐 | 相等 |
| category 数量一致 | 按 rule_id 分组，源/目标 category 条数对齐 | 相等 |
| `category_id` 非空有效 | 目标 category `category_id` 关联 `n_material_template.id` 能命中 | 无 0 或断裂 |
| 状态映射正确 | 目标表 `state` 值域 = {0,1,3}（无 2） | 成立 |
| 无重复 rule | 目标表按 `src_rule_id` 标记去重计数 = 源计数 | 成立 |

### 7.3 业务回归验证

- 选一条迁移后的 rule，按排程查询链路 `unit → category.category_id → n_material_process_define(template_id) → process_code` 端到端走通。
- 对比同一 rule 在八合一与排程侧的查询结果是否语义一致。
- 在测试环境走前端页面，确认排程配置页能正确展示迁移过来的零售规则。


## 8. 迁移后自测方案

> **目标**：验证八合一零售配置已正确、完整地迁移到排程表，查询链路打通，业务功能无回归。
> **范围**：仅针对本次迁移的零售 rule（`sale_type=2`），不含整装。
> **通过准则**：§8.2–§8.5 全部用例通过，且与迁移日志表 `migrate_rule_log` 记录一致。

自测分四层逐层验证，任一层失败即阻断下层：

| 层级 | 验证对象 | 方法 | 责任人 |
|---|---|---|---|
| 数据层（§8.2） | 数据数量与字段映射正确性 | SQL 对账 | 开发 |
| 链路层（§8.3） | 排程查询链路端到端打通 | 手工/脚本查询 | 开发 |
| 业务层（§8.4） | 排程配置页与下游功能正常 | 前端走查 | 开发+测试 |
| 异常层（§8.5） | 失败 rule、边界值处理 | 日志表+抽样 | 开发 |


### 8.1 自测环境与前置准备

| 项 | 要求 | 用途 |
|---|---|---|
| 环境 | 测试库（独立于生产），已跑完一轮完整迁移 | 安全验证，不影响生产 |
| 源数据快照 | 迁移时刻的源 `material_flow_rule*` 数据已 dump | 对账基准，防止迁移期间源表变动 |
| 日志表 | `migrate_rule_log` 本次批次记录完整 | 统计成功/失败数、新旧 id 映射 |
| 批次号 | 记录本次迁移 `batch_no`（如 `20260903_01`） | 所有校验 SQL 按批次过滤 |
| 排程配置 | `delivery_process_cfg_id` 已确定并填入 | rule 主表关联校验 |
| 样本 rule | 从日志表抽 3 条成功 rule（大/中/小 category 数） | §8.3、§8.4 深度走查 |

前置检查 SQL：

```sql
-- 本次批次迁移概况
SELECT status, COUNT(*) AS cnt
FROM migrate_rule_log
WHERE batch_no = '20260903_01'
GROUP BY status;   -- 0=成功，1=失败
```


### 8.2 数据层自测（SQL 校验）

#### 用例 T1：rule 数量一致

```sql
-- 源成功迁移数 = 日志表成功数 = 目标新建数
SELECT
  (SELECT COUNT(*) FROM migrate_rule_log
     WHERE batch_no='20260903_01' AND status=0) AS log_success,
  (SELECT COUNT(DISTINCT src_rule_id) FROM migrate_rule_log
     WHERE batch_no='20260903_01' AND status=0) AS log_src_distinct,
  (SELECT COUNT(*) FROM delivery_flow_rule d
     WHERE d.id IN (SELECT dst_rule_id FROM migrate_rule_log
        WHERE batch_no='20260903_01' AND status=0)) AS dst_rule_cnt;
-- 预期：三列相等
```

#### 用例 T2：unit / category 数量按 rule 对齐

```sql
-- 逐 rule 比对源/目标 unit、category 条数
SELECT l.src_rule_id, l.dst_rule_id,
   (SELECT COUNT(*) FROM material_flow_rule_unit u WHERE u.rule_id=l.src_rule_id) AS src_unit,
   (SELECT COUNT(*) FROM delivery_flow_rule_unit u WHERE u.rule_id=l.dst_rule_id) AS dst_unit,
   (SELECT COUNT(*) FROM material_flow_rule_category c WHERE c.rule_id=l.src_rule_id) AS src_cat,
   (SELECT COUNT(*) FROM delivery_flow_rule_category c WHERE c.rule_id=l.dst_rule_id) AS dst_cat
FROM migrate_rule_log l
WHERE l.batch_no='20260903_01' AND l.status=0;
-- 预期：src_unit=dst_unit，src_cat=dst_cat（每行）
```

> 可加 `HAVING src_unit<>dst_unit OR src_cat<>dst_cat` 快速定位不一致 rule。

#### 用例 T3：状态映射正确（值域={0,1,3}，无 2）

```sql
SELECT 'rule' AS tbl, state, COUNT(*) FROM delivery_flow_rule
  WHERE id IN (SELECT dst_rule_id FROM migrate_rule_log WHERE batch_no='20260903_01' AND status=0)
  GROUP BY state
UNION ALL
SELECT 'unit', state, COUNT(*) FROM delivery_flow_rule_unit
  WHERE rule_id IN (SELECT dst_rule_id FROM migrate_rule_log WHERE batch_no='20260903_01' AND status=0)
  GROUP BY state
UNION ALL
SELECT 'category', state, COUNT(*) FROM delivery_flow_rule_category
  WHERE rule_id IN (SELECT dst_rule_id FROM migrate_rule_log WHERE batch_no='20260903_01' AND status=0)
  GROUP BY state;
-- 预期：state 只出现 0/1/3，不能出现 2（删除）
```

#### 用例 T4：category_id 映射有效（R1 核心校验）

```sql
-- 目标 category 的 category_id 必须能命中 n_material_template.id，且不为 0
SELECT c.dst_rule_id, c.id AS dst_cat_id, c.category_id, t.id AS tpl_id, t.name
FROM delivery_flow_rule_category c
LEFT JOIN n_material_template t ON t.id = c.category_id
WHERE c.rule_id IN (SELECT dst_rule_id FROM migrate_rule_log
    WHERE batch_no='20260903_01' AND status=0)
  AND (c.category_id = 0 OR t.id IS NULL);
-- 预期：0 行。若有结果=映射断裂，对照 R1 失败原因
```

#### 用例 T5：document_type 回填正确

```sql
-- 排程 unit 的 document_type 应等于 sale_type，document_type_name 应等于 sale_type_name
SELECT u.rule_id, u.sale_type, u.document_type,
       u.sale_type_name, u.document_type_name
FROM delivery_flow_rule_unit u
WHERE u.rule_id IN (SELECT dst_rule_id FROM migrate_rule_log WHERE batch_no='20260903_01' AND status=0)
  AND (u.document_type <> u.sale_type OR u.document_type_name <> u.sale_type_name);
-- 预期：0 行
```

#### 用例 T6：丢弃字段确未写入 / 补充字段已写入

```sql
-- 排程独有字段已补
SELECT COUNT(*) AS bad_rule_cfg FROM delivery_flow_rule
WHERE id IN (SELECT dst_rule_id FROM migrate_rule_log WHERE batch_no='20260903_01' AND status=0)
  AND (delivery_process_cfg_id=0 OR delivery_process_cfg_state<>1);
-- 预期：0

-- 排程独有 delivery_template_id 本次应留 -1
SELECT COUNT(*) AS bad_tpl FROM delivery_flow_rule_category
WHERE rule_id IN (SELECT dst_rule_id FROM migrate_rule_log WHERE batch_no='20260903_01' AND status=0)
  AND delivery_template_id <> -1;
-- 预期：0（本次不写）

-- 八合一独有字段在排程不应出现：排程表无 merchant_id/work_type/is_show 列，无需校验
```

#### 用例 T7：无重复迁移（幂等）

```sql
-- 日志表每个 src_rule_id 只能一条成功记录
SELECT src_rule_id, COUNT(*) AS c
FROM migrate_rule_log
WHERE batch_no='20260903_01' AND status=0
GROUP BY src_rule_id HAVING c > 1;
-- 预期：0 行（唯一索引已保证）
```


### 8.3 链路层自测（查询链路打通）

验证排程侧完整查询链路能走通：`unit → rule → category.category_id → n_material_process_define(template_id) → process_code`。

#### 用例 L1：单 rule 端到端查询

从日志表抽 1 条成功 rule，验证从维度定位到 process 全链路：

```sql
SET @dst_rule := <某 dst_rule_id>;

-- Step1: 由维度(unit 的 sale_type/document_type=2 + 分公司+套餐)定位到 rule
SELECT id, delivery_process_cfg_id FROM delivery_flow_rule WHERE id=@dst_rule;

-- Step2: rule → category，取 category_id
SELECT id, category_id, material_code, supplier_code
FROM delivery_flow_rule_category WHERE rule_id=@dst_rule;

-- Step3: category_id 作为 template_id 查 n_material_process_define 的 process_code
SELECT p.template_id, p.process_code, p.task_type, p.state
FROM n_material_process_define p
WHERE p.template_id IN (
    SELECT category_id FROM delivery_flow_rule_category WHERE rule_id=@dst_rule
);
-- 预期：能查到 process_code，非空
```

#### 用例 L2：源/目标链路结果一致性

同一 rule 在八合一与排程侧，按相同维度查询，最终拿到的 process_code 集合应语义一致：

```sql
-- 源侧 process（经 mapping → n_template → process_define）
SELECT DISTINCT p.process_code
FROM material_flow_rule_category c
JOIN material_flow_rule_template_mapping m ON m.rule_id=c.rule_id AND m.rule_category_id=c.id
JOIN n_material_template t ON t.template_code=m.n_template_code
JOIN n_material_process_define p ON p.template_id=t.id
WHERE c.rule_id=<src_rule_id>;

-- 目标侧 process（经 category.category_id 直接作 template_id）
SELECT DISTINCT p.process_code
FROM delivery_flow_rule_category c
JOIN n_material_process_define p ON p.template_id=c.category_id
WHERE c.rule_id=<dst_rule_id>;
-- 预期：两结果集 process_code 一致
```

> 这是本次迁移最关键的语义校验——证明目标 `category_id` 承接的 `n_material_template.id` 与源链路算出来的是同一个模板。


### 8.4 业务层自测（功能回归）

在测试环境前端走查迁移过来的零售规则，覆盖以下场景：

| 用例 | 操作 | 预期 |
|---|---|---|
| B1 列表展示 | 排程配置列表页，按零售单据类型筛选 | 能查到本次迁移的 rule，数量与日志表成功数一致 |
| B2 详情查看 | 打开某条迁移 rule 的详情 | rule/unit/category 三层数据展示正确，与源侧一致 |
| B3 维度查询 | 按分公司+套餐+零售类型查询排程规则 | 命中迁移的 rule，查询结果与源侧同维度结果一致 |
| B4 状态展示 | 查看迁移 rule 的状态字段 | 有效 rule 显示"有效"，状态值域正确（无"删除"误显） |
| B5 下游消费 | 触发依赖排程规则的业务流程（如排程下单/任务生成） | 能正常读取 process_code，不报"模板不存在/配置缺失" |
| B6 新增/编辑 | 对迁移 rule 做一次编辑保存 | 不影响已有迁移数据，修改正常落库（验证迁移数据可被业务正常读写） |
| B7 失败 rule 处理 | 查 §8.5 失败 rule，业务侧表现 | 失败 rule 不应出现在排程配置页（已回滚），日志表有记录 |

> B5 是关键——如果下游业务报模板缺失，说明 §8.3 的 L2 链路实际未通，需回查 `category_id` 映射。


### 8.5 异常与边界自测

针对失败 rule 和边界数据，验证迁移机制的健壮性。

| 用例 | 场景 | 校验方法 | 预期 |
|---|---|---|---|
| E1 失败 rule 已回滚 | 日志表 `status=1` 的 rule | `SELECT * FROM migrate_rule_log WHERE status=1`，再查目标表 `delivery_flow_rule WHERE id=dst_rule_id` | 目标表无对应记录（dst_rule_id=0 或不存在），无半成品 |
| E2 失败原因可读 | 失败记录的 error_msg | 抽样查看 | 原因明确（如 `n_template_code 缺失`、`模板不存在`、`源 rule 不存在`） |
| E3 重复迁移幂等 | 对已成功 rule 重跑迁移 | 重跑后查日志表该 src_rule_id | 仍是 1 条成功记录，目标表不产生重复（唯一索引拦截或代码跳过） |
| E4 NULL/超长脏值 | 源 `material_name` 为 NULL、`node_process_name` 超长 | 抽样比对源/目标 | NULL→''，超长字段在排程（容量更大）正常写入 |
| E5 大 category 量 rule | 选 category 数最多的 rule | 单 rule 事务完成、无超时 | 30s 内完成，状态=成功 |
| E6 状态重映射边界 | 源含 state=0/2 的 rule（若 Q3 选全量迁） | 查目标 state | 0→0、2→3，无落到 2（删除） |
| E7 批次回滚验证 | 按 §6.2 对测试库某批次执行回滚 SQL | 回滚后查目标三表 + 日志表 | 本批次目标数据清空，日志记录保留（审计） |

> E1、E3、E7 是迁移机制本身正确性的核心保证，测试环境必须全部通过。


### 8.6 自测结论模板

自测完成后填写下表，作为迁移上生产的前置卡点：

| 自测层 | 用例 | 结果 | 备注/失败用例 |
|---|---|---|---|
| 数据层 | T1 数量一致 | ☐通过 ☐失败 | |
| | T2 unit/category 对齐 | ☐通过 ☐失败 | |
| | T3 状态值域 | ☐通过 ☐失败 | |
| | T4 category_id 有效 | ☐通过 ☐失败 | |
| | T5 document_type 回填 | ☐通过 ☐失败 | |
| | T6 字段补/丢正确 | ☐通过 ☐失败 | |
| | T7 无重复 | ☐通过 ☐失败 | |
| 链路层 | L1 单 rule 端到端 | ☐通过 ☐失败 | |
| | L2 源/目标 process 一致 | ☐通过 ☐失败 | |
| 业务层 | B1–B7 功能回归 | ☐通过 ☐失败 | |
| 异常层 | E1–E7 机制健壮性 | ☐通过 ☐失败 | |

**总体结论**：☐ 全部通过，可上生产　☐ 存在失败，阻断上线，待修复后复测

- 迁移统计：成功 `____` 条，失败 `____` 条，失败明细见日志表 `batch_no=________`。
- 遗留风险/待确认：________
- 自测人 / 日期：________ / ________



## 9. 待确认问题清单

以下问题需业务/产品确认后方案才能定稿执行：

| 编号  | 问题                                             | 默认假设               | 确认后影响                            |
| --- | ---------------------------------------------- | ------------------ | -------------------------------- |
| Q1  | 迁移边界：纯零售 vs 零售类（含搭售等）                          | 纯零售（`sale_type=2`） | 决定源数据过滤 SQL                      |
| Q2  | `delivery_process_cfg_id` 来源：新建 vs 复用          | 待定                 | rule 主表必填，无此 id 无法写入             |
| Q3  | 失效/编辑态数据是否迁移                                   | 只迁 `state=1`（有效）   | 若全量迁需启用状态映射 0→0、2→3              |
| —   | `delivery_template_id` 本次是否写入                  | 不写，留 `-1`          | 用户已明确，二期再补                       |
| —   | `n_template_code` 查不到时：跳过该 rule 还是置 0      | 跳过该 rule，记 `status=1`+原因（见 R1） | 影响 category 的 `category_id` 取值策略 |
| —   | 迁移日志表 `migrate_rule_log` 是否随生产保留 | 保留作审计映射 | 已确定用日志表反查回滚，不再污染业务表 |
| —   | 迁移执行环境与时间窗                                     | 测试库 → 预发 → 生产低峰    | 影响上线节奏                           |

> 以上确认后，更新本文档对应章节即可形成可执行方案。


---
---
